@AGENTS.md

# personal-site — 项目上下文

> **这份文档同时写给人和 AI。** Claude Code 每次会话会自动把它加载进上下文，所以它是跨会话记忆的载体——对话记录会丢，这里不会。
>
> **维护约定**：做了新决策、完成了里程碑、或发现了新陷阱，就更新本文件，并同步「最后更新」日期。决策只增不删——推翻旧决策时把它标记为「已废弃」并说明原因，保留推理链比保留结论更有价值。

**最后更新**：2026-08-19

---

## 1. 项目是什么

Yixuan "Coda" Shi（GitHub [@Coda-Shi](https://github.com/Coda-Shi)）的个人网站。

| 项 | 值 |
|---|---|
| 仓库 | [Coda-Shi/personal-site](https://github.com/Coda-Shi/personal-site)（Public） |
| 本地路径 | **因机器而异**，见 §6。已知：`C:\Users\shiyi\dev\personal-site`、`C:\Users\Yixuan Shi\dev\personal-site` |
| 主分支 | `main`（受保护） |
| 托管 | Vercel（scope: `coda4`） |
| **正式域名** | **https://yixuancodashi.com** （2026-08-15 上线） |
| www | `www.yixuancodashi.com` → **308** → 根域名。根域名是主，不要反过来，见 D20 |
| Vercel 生产别名 | `personal-site-kappa-eight-52.vercel.app` → 307 → 正式域名。**不是** `personal-site-coda4.vercel.app`（那个是次级别名，Vercel 给它加 `X-Robots-Tag: noindex`） |
| 开发环境 | Windows 11、PowerShell、Node v24.18.1、npm 11.16.0 |

## 2. 技术栈

- **Next.js 16.3**（App Router，Turbopack）
- **React 19.2**
- **TypeScript 5**
- **Tailwind CSS 4**
- **ESLint 9**（`eslint-config-next`）

目录结构为 `src/` + App Router，路径别名 `@/*` → `src/*`。

## 3. 关键决策记录

> 格式借鉴 ADR（Architecture Decision Record）。**每条决策都必须写"代价"**——只记好处的决策记录会诱导后来者（包括 AI）以为这是无损选择，从而在该重新权衡时不重新权衡。

### D1 — 用 Next.js，而非 Astro 或纯 HTML ✅ 生效中

**决策**：Next.js + TypeScript + Tailwind。

**理由**：
1. 项目所有者以 vibe coding 为主，几乎不手写代码 → **「AI 对该框架有多熟」是一等决策因素**。Next.js 是文档和社区示例最多的 React 框架，生成代码的准确率显著更高。
2. Vercel 是 Next.js 的开发公司，部署零配置，契合度无可替代。
3. 不会撞天花板——将来要加表单、API、后台、AI 接口都不用换框架。
4. TypeScript 让错误在编辑器和构建期暴露，而不是等浏览器白屏。**对无法靠阅读代码发现 bug 的使用者，这层自动校对的价值被放大。**

**代价**：概念负担明显重于 Astro（服务端组件 vs 客户端组件、路由约定、缓存语义）。对纯展示型网站属于杀鸡用牛刀。这是为「长期可扩展」主动付出的成本，**不是没意识到**。

### D2 — 托管选 Vercel ✅ 生效中

**决策**：Vercel Hobby（免费）。

**理由**：与 D1 的框架选择相互强化；PR 预览部署对「改完想先看看效果」这个高频动作帮助极大；将来需要服务端能力时不用迁移。

**代价**：Hobby 计划**条款上禁止商业用途**。一旦网站要挂广告、卖东西或用于商业推广，必须升级付费计划。

**备选**：Cloudflare Pages（带宽无限、性能更好，但生态不如 Vercel 顺手）。

### D3 — 仓库设为 Public ✅ 生效中

**理由**：分支保护和 CI 分钟数在公开仓库上免费，私有仓库需要 GitHub Pro（$4/月）；网站代码本来就会公开部署，藏不住；同时充当作品集。

**代价**：提交历史永久可被爬取 → 直接导致了 D4。

### D4 — 提交邮箱使用 GitHub noreply ✅ 生效中

**决策**：本仓库的 `user.email` 设为 `<ID>+Coda-Shi@users.noreply.github.com`（repo 级配置，非全局）。

**理由**：公开仓库的提交历史会被爬虫永久收录，真实邮箱一旦推上去**无法事后撤回**。noreply 地址照样能让提交归属到 GitHub 头像和贡献格子。

**执行细节**：`create-next-app` 生成的头两个提交带的是真实邮箱，已在**首次推送前**用 `git rebase --root --exec "git commit --amend --no-edit --reset-author"` 重写。哈希因此全变（`845584c` → `1c3a3b2`）。

> ⚠️ **给未来的 AI**：全局 `user.email` 仍是真实邮箱。**再帮他新建任何公开仓库时，必须在首次推送前设置 noreply**，否则真实邮箱泄漏且不可逆。

### D5 — 分支保护开启，但管理员可豁免 ✅ 生效中

**决策**：`main` 要求 PR + `build` 检查通过 + 禁止 force push/删除，但 `enforce_admins = false`。

**理由**：所有者是 git 新手。第一天就用硬约束把他锁在门外会造成挫败，且单人项目无人可批准 PR。留逃生口让默认路径是 PR、紧急时仍能直推。

**代价**：对所有者本人而言这是护栏而非硬约束——真正的强制只对未来的协作者生效。**这是刻意为之，不是配置遗漏。**

**何时重新评估**：等所有者对 PR 流程熟练后，或有真实协作者加入时，改为 `enforce_admins = true`。

### D6 — 域名暂不购买 ✅ 已执行（2026-08-15 买入 `yixuancodashi.com`）

> **结果**：按本条的选购原则在 **Porkbun** 买了 `yixuancodashi.com`，$11.06/年，首年与续费同价，WHOIS 隐私免费且**已验证生效**（公开 RDAP 只返回 Porkbun 自己，registrant 一栏完全不公开）。NS 留在 Porkbun，只用 A/CNAME 指向 Vercel——**没有**把 nameserver 交给 Vercel 托管。绑定过程踩的坑见 D20。原推理保留如下。


**理由**：先用免费的 `*.vercel.app`，等网站成型再决定。**绑定自定义域名的迁移成本为零**，随时可加。

**已调研**：域名费与 Vercel 无关，是付给注册商的独立开销。倾向 **Porkbun**（.com 约 $11.08/年，首年与续费同价，DNS 可自由指向）。Cloudflare Registrar 更便宜（$10.46，按批发成本零加价）但强制使用其 DNS。

> 💡 **选购原则**：看续费价，不看首年价。GoDaddy 等首年 $1–5、次年 $20–25。后缀的影响远大于注册商——`.com` ≈ $11、`.dev` ≈ $12–15、`.io` 高达 $40–60。

## 4. 内容与设计方向

> 2026-08-07 由所有者口述。**这是约束条件，不是参考建议**——审美没有客观对错，只有是否符合他的意图，所以本节优先于任何默认审美倾向。

### 自我定位：三条职业线环绕一个中心

1. **Scholarly work** — 学术
2. **Professional work** — 职业
3. **Creative Artist and Leader** — 创作者与领导者

三条线**并列，不分主次**——不得把任何一条降级为附属栏目或塞进「其他」。

中心是他的照片，点进去是 **Coda himself, as a writer and advocate**（`/coda`）。空间关系本身承载语义：**向外是面向世界的三种职业身份，向内是他本人。**

- **writer** 明确指他自己的**诗文**——他的原话是「内心最深处最真实的感情，独立于社会之外」。**不是**指游戏剧本（属 Creative）或学术写作（属 Scholarly）。
- **advocate** 收纳 Crisis Text Line 危机咨询、浔正心理诊所热线、宁波领养日动物救助。这些放进任何一条职业线都会失真。

> ⚠️ 诗文只能由本人提供，AI 不得代笔或生成占位诗句。`/coda` 的 Poems 区块目前是明确的空状态。

### 兴趣

心理学 · 哲学 · 金属核（metalcore）

### 审美

| 维度 | 方向 |
|---|---|
| 核心张力 | **「现代色彩 + 几何极简」× 「古典典雅」的对撞** —— 不是二选一，是刻意让两套视觉语汇同框冲突 |
| 色彩 | 黑 / 灰 / 白 打底，叠加红 / 蓝 / 黄三色。2026-08-07 细化为**深沉色**而非高饱和原色，见 D7 |
| 元素 | 偏爱**各类符号**（symbols）作为视觉语言，已落实为 D9 的记号系统 |

> ⚠️ **给未来的 AI**：不要默认输出「干净的 SaaS 风」——柔和渐变、圆角卡片、低饱和中性色、通用无衬线字体，正是他偏好的**反面**。金属核 + 哲学/心理学的组合意味着高对比、严肃、甚至略带攻击性的表达是被允许的。

### 语言

> ⚠️ **「站点内容一律英文」已于 2026-08-09 废弃**，被 D16 取代。原文保留如下，因为它解释了初版为什么全英文。

~~**站点内容一律英文。** 本文档和提交信息中文/英文均可，但用户可见的页面文案是英文。~~

**现行**：站点为中英双语，英文是**规范版本**（canonical），中文逐节翻译。本文档和提交信息中英均可。见 D16。

### 素材

简历原件 `Yixuan_Shi_Main_Resume.docx` 位于仓库根目录，**已 gitignore，不会进入公开历史**（见 §7 的隐私规则）。文本内容已抽取进 `src/lib/content.ts` 作为单一数据源——改简历改那里，不要在页面里散落硬编码。

### D7 — 色盘用深沉色 ✅ 生效中（底色部分已于 2026-08-07 被 D10 推翻）

> ⚠️ **本条原本还规定「底色用纸白」，已废弃。** 所有者选择了 trinity 圆盘方案，其核心交互是「选中的颜色扩展到页面边缘」——这需要一个能被颜色吞没的暗底。**深沉色这一半仍然生效，纸白底那一半不再生效**，当前色值见下表与 D10。保留原推理是因为它解释了为什么色相是这三个。

**决策**：

| 角色 | 色值 | 用途 |
|---|---|---|
| 虚空 void | `#050505` | 页面底色 |
| 骨白 bone | `#F2EFE9` | 正文 |
| **克莱茵蓝** | `#002FA7` | Scholarly —— IKB 真值 |
| **酒红** | `#6E1423` | Creative —— 酒红真值 |
| **旧金** | `#6A5216` | Professional —— **唯一不能用真值的** |

> 2026-08-07 二次加深：所有者反馈「不够深沉」，克莱茵蓝与酒红已回到颜料真值。**旧金不能回到 `#B08D2E`**——黄色相的固有亮度极高，真值会比另外两色亮约 6 倍，三位一体立刻塌成一主二从。压到发铜正好是「旧金」的字面意思。**调这三个值时必须重新核对相对亮度，不能只看色相好不好看。**

**理由**：所有者给的候选是「深沉色（酒红/克莱茵蓝/旧金）」与「低饱和/莫兰迪」。选前者，因为**莫兰迪的全部功能是让颜色互相和解**，而本站的核心概念是对撞——用莫兰迪会直接消解掉 §4 那条张力，并让站点落入通用设计师作品集的样貌。

克莱茵蓝是整套色盘的锚点：群青在中世纪是青金石研磨的圣像颜料，1957 年被 Yves Klein 变成单色画的现代主义宣言。**同一个颜色同时是古典的和现代的**——概念由颜料本身承载，不需要靠额外元素制造。

底色选纸白而非黑，这条最反直觉：**克莱茵蓝在暗底上会死掉**（Klein 的作品全部挂在白墙上），金色配黑是奢侈品广告的滥调。深色颜料需要亮底才显出它们是颜料。

**代价**：三色在黑底上必须偏离原值。纯 `#002FA7` 放在 `#050505` 上会发闷，所以提亮；`#B08D2E` 则比另外两色亮太多，会压过它们、破坏「三者平等」，所以压暗。**这不是随意调色，是为了让三条线的感知亮度接近。**改色值时必须重新核对这个平衡。

### D8 — 三套字体构成排版对撞 ✅ 生效中

**决策**：`Cormorant Garamond`（古典衬线，含斜体）× `Space Grotesk`（几何无衬线）× `JetBrains Mono`（数据、日期、标签）。

**理由**：不是常规的「衬线标题 + 无衬线正文」（那太和谐），而是让两套语汇在相近字号上并置，使冲突可见。Mono 承载一切属于数据的东西（样本量、年份、标签），呼应他心理测量学的一面。

经 `next/font` **自托管**，访客浏览器不向 Google 发任何请求——顺带解决隐私问题。

**代价**：三套字体比一套重。Cormorant 在小字号下偏纤细，只用于展示级字号。

### D9 — 记号系统取自他自己的学科 ✅ 生效中

| 线 | 记号 | 形式 | 来源 |
|---|---|---|---|
| Scholarly | **⊨** | SVG 绘制 | 模型论的语义蕴涵，`M ⊨ φ`「结构 M 满足 φ」。验证性因子分析问的就是模型是否满足数据；模型论又是形式语义学的核心，直通维特根斯坦 |
| Professional | **§** | 字体 | 章节号——政策、合规、SOP 的记号 |
| Creative | **𝄌** | SVG 绘制 | 终曲记号。他的名字就是 Coda。按所有者提供的参考图：椭圆环**左右粗、上下细**（衬线体大写 O 的笔画调制，横向 2.8 / 纵向 1.5），加两条贯穿的细线。**用双椭圆 `fill-rule="evenodd"` 填充实现——`stroke` 只能给出均匀粗细，做不出调制** |
| `/coda` | **无** | — | 见下 |

> ⚠️ **Cormorant Garamond 不含任何逻辑/数学记号。** 实测字宽：`⊨` `∀` `◇` `∴` `Λ` `χ` 全部回退到通用衬线——Windows 上是 Times New Roman，其他系统各不相同，某些环境可能直接缺字。**初版用 ∀ 和 ◇ 时它们从来就不是 Cormorant 渲染的。**
>
> 所以 ⊨ 和 𝄌 一律 **SVG 绘制**（`TrackMark` 组件），⊨ 做竖粗横细的调制以呼应衬线体的笔画对比。**§ 是例外，保留字体**——它是真正的排印记号，Cormorant 里有且很漂亮，印刷术自己的东西应该被排出来而不是被描出来。
>
> 加新记号前先用 canvas `measureText` 对比该字符在 Cormorant 与通用衬线下的字宽，**相等即说明字体没有这个字**。

**理由**：他明确偏爱符号，而他的学科（符号逻辑、心理测量）本身就有记号法。用真实记号而非通用图标，是让站点无法被认成模板的关键。

**中心不编码**：`/coda` **不使用任何轨道色、不使用任何记号**，并采用更窄的行宽和更多衬线。三个社会身份被编码，最私密的自我不被编码——这是「独立于社会之外」的视觉落实，是刻意的，不是遗漏。

> 🔴 **首页中心的头像必须是彩色。不要以 D9 为由把它改成黑白。**
> **黑白肖像在中文语境里读作遗像**，而本站有中文版——这不是可以拿去和构图权衡的偏好，是图像本身携带的语义。
>
> 当初选单色的理由（彩色照片会给「颜色即轨道标识」的体系引入第四种颜色）也站不住：照片不是色块，不与三种颜料竞争；D9 对中心的真正要求是**没有克莱茵蓝／酒红／旧金**，彩色人像并不违反。
>
> `scripts/hub-portrait.py` 仍会输出 `portrait-mono.png` 供比对，但它已 gitignore，**只有 `portrait.png`（彩色）上线**。原图放 `_incoming/`，同样 gitignore。

### D10 — 首页是 trinity 圆盘，选中即整页吞没 ⚠️ 几何部分已被 D27 取代（水漫转场仍然生效）

> 🔴 **本条描述的「一个圆环分成三个 120° 扇区」已于 2026-08-19 换成三个交叠的圆（Venn），见 D27。**
> 「选中即整页吞没」「目的地页面底色就是那个颜色」「圆盘落在视口正中」「文字块必须限宽」「左上角简介淡出」「真正的链接是 HTML `<Link>`」——**这些全部仍然生效**，D27 只换了几何。下面凡是提到扇区、conic-gradient、内弧、锥尖的实现细节都已作废。


**决策**（2026-08-07 由所有者提出，推翻了初版的三角构图）：

首页是一个巨大的圆环，**三条线各占 120°**，中心留空放照片（`/coda` 入口）。悬停或聚焦某一扇区时，该色**沿该扇区自己的 120° 方向射出**至页面边缘；其余两扇区降到 40% 不透明度。

**圆盘必须落在视口正中**（打开即见，页面不滚动）。为此页眉页脚脱离文档流绝对定位——放回文档流会把圆盘往下推。已在 1440×900 / 1280×720 / 1024×768 / 375×812 / 812×375 验证中心偏移为 0,0 且不滚动。

> ⚠️ 首页所有文字块必须 `w-fit` 或限宽。**全宽块级盒子在左上角会伸到居中圆盘的下方**，即使可见文字离得很远——这会让碰撞完全无法推理，也会让自动检测报假阳性。视口高度 < 600px 时隐藏简介段落（`.hide-when-short`），横屏手机塞不下。

**关键点：颜色扩展不是装饰，是转场。** 点进去之后，那条线的页面**整页底色就是那个颜色**——扩展动画是在预告目的地长什么样。`/coda` 和 `/cv` 保持黑底。

**实现约束**：
- 扇区是 SVG 环形扇形，路径由 `annularSector()` 按角度算出。三段路径长度必须相等（当前为 767），**不等就说明角度算错了**，这是最容易出错也最容易验证的地方。
- 放射用 `conic-gradient` 而非 `clip-path`：前者能给出精确的角度切片，后者需要手工用多边形逼近圆弧。`conic-gradient` 从 12 点起算、扇区角度从 3 点起算，**两者相差 90°**，改角度时别忘了这个偏移。
- **扇区点亮时，左上角的简介与提示语淡出**（标题保留）。焦点状态因此住在 `HomeStage` 而不是 `TrinityDisc`——圆盘和简介两边都要响应它。
  - **不要改成用光束盖住文字。** Scholarly 楔形朝上，斜边正好从段落中间切过，盖住会变成半段消失半段还在，看起来像渲染故障而不是设计。
  - 出场 260ms、回来 620ms：光束 700ms 到位，文字必须先撤走；但回来太快会「啪」一下。
  - ⚠️ **入场动画和淡出过渡不能挂在同一个元素上。** 带 `fill-mode: both` 的动画跑完会把 opacity 钉死在终值，`transition` 完全没有话语权。现在动画在外层 `div`、过渡在里层 `p`。

- **光束必须看起来从扇区自己的内弧射出，不是从圆心。** conic 扇形天然收敛于圆心，锥尖会顶在中心圆边上。解法是在圆盘 SVG 的**最前面**画一个 `r = R_INNER` 的 void 色实心圆遮住锥尖——它同时也免费定义了生长动画的形状（光束在越过这个半径之前完全不可见）。**别删这个圆**，删了锥尖就回来了。
- **可访问性**：真正的链接是覆盖在扇区上的 HTML 标签（`<Link>`），它们承载可访问名称、无 JS 时可用、聚焦时触发同样的水漫。SVG 路径上的 `onClick` 只是指针设备的增强，不是唯一入口。**改这块时不要把 `<Link>` 换成 SVG `<a>`。**
- 文字一律用 `text-bone/xx`，不用固定灰阶——底色会随路由变成三种颜色之一，固定灰阶只在黑底上成立。

**代价**：首页从纯静态变成了客户端组件（需要 `useState`）。这是交互本身要求的，无法避免。

### D11 — 入场是「描线落笔」，圆盘被绘制而非淡入 ✅ 生效中

**决策**：三个扇区依次被**描线画出**，颜色随后渗入线内。页眉文案先于圆盘，页脚最后。全程约 1.9 秒。

**实现约束**：
- 描线用 `pathLength="1"` + `stroke-dasharray: 1`，动画 `stroke-dashoffset: 1 → 0`。**不要改回按实际路径长度计算**——`pathLength` 归一化后这套关键帧对任何几何都成立，也避免了在 keyframe 里解析 CSS 自定义属性（Safari 历史上有 bug）。
- 所有入场动画必须用 `animation-fill-mode: both`：延迟期间停在隐藏态，跑完后停在可见态。**漏掉 `both` 会让内容在动画结束后闪回不可见。**
- 全局的 `prefers-reduced-motion` 规则把时长压到 0.01ms，配合 `both` 会直接落在完成态而不是落在空白——改动画时别破坏这个性质。
- 验证方法：`document.getAnimations().forEach(a => a.finish())`，然后检查所有元素的 opacity / fill-opacity / dashoffset 是否都在可见终态。**这能在无法观察动画过程的环境里验证唯一致命的失败模式。**

### D12 — 刻度环与数字对撞 ⚠️ 两半都已改写（刻度环 2026-08-16 移除；数字对撞 2026-08-17 改为同族内对比）

> 🔴 **刻度环已于 2026-08-16 按所有者要求移除**（原话：「外面那一圈星座坐标我突然不太喜欢了」）。下面这段保留是为了说明它当初为什么在，以及移除它牵动了什么——**不要照着它重新加回来**。
>
> 连带影响：D11 的入场节奏里「仪器先在」那一拍没有了，扇区描线的起始延迟从 350ms 提到 200ms，否则开场是一段空屏。OG 卡片也同步去掉了刻度（`scripts/brand-images.py`），卡片和页面必须是同一个标记。D13 的 `R_MIN = 580` 当初是按「圆盘外刻度最远到 549 画板单位」算的，现在圆盘外缘只到 `R_OUTER`，这个值因此**偏保守但仍然安全**，没有改动。

~~**刻度环**~~：外缘每 3° 一条短刻度、每 15° 一条长刻度，合成**单条 path**（144 条独立 `<line>` 是 144 个永远不需要单独寻址的节点）。星盘/游标卡尺的语汇，用绝对精确的几何画出。它同时是未来符号层的载体。

**数字对撞**（2026-08-17 改写）：原本是 `.oldstyle`（衬线老式数字）对 `.label`（**等宽** tabular lining figures），跨两个字族。所有者要求统一视觉语言后，`.label` 改用 display 族，于是对撞变成**同一字族内的两种数字样式**——`.oldstyle` 的老式数字对 `.label` 的 lining 数字。同一个年份仍然读作两种写法，但现在是一个传统里的两种惯例，而不是两套互不相干的系统。**这是弱化，不是取消**；实测两处 `font-variant-numeric` 仍然不同。 若 Google 的 Cormorant 构建不含 `onum` 特性，此设置静默失效、退回默认数字，不会出错。

### D13 — 符号层画在固定底板上 ⚠️ 画板尺寸与扇形约束已被 D27 取代（其余生效中）

> 🔴 **2026-08-19：符号层不再被遮罩到某个 120° 扇区，横板也不再是 2000×2000 正方形。** 见 D27。
> 「固定底板整块等比缩放，排好一次永远排好」「三级层次、字号由目标渲染宽度反解」「放不下就丢」「不得重复」「必须用 `meet` 不能用 `slice`」「验证要检查可见而非存在」「排版字宽与碰撞字宽两张表」——**全部仍然生效**。作废的只有：扇区角度约束（`insideWedge`）、`R_MIN = 580` 的推导过程（数值本身仍然是 580，但理由换了）、以及横板的 2000×2000。


**决策**：符号层是一个 `viewBox="0 0 2000 2000"` + `preserveAspectRatio="xMidYMid slice"` 的 SVG，**不是**用视口单位定位的一堆 div。

**理由**：初版每个元素独立用 `vmin` 定位，结果是**每换一个视口就重新洗一次牌、堆出不同的重叠**。固定底板整块等比缩放，元素之间的相对位置在任何分辨率下完全一致——**排好一次就永远排好**。

**布局算法**（`layout()`）：每个元素最多试 80 个候选位置，必须同时满足
1. 盒子四角全部落在本扇区的 120° 内（否则会被 conic 遮罩切掉半个字）
2. 与所有已放置的盒子保持 ≥16 单位间距

**找不到位置的元素直接丢弃**——空隙看起来像构图，重叠看起来像 bug。

Creative 扇区的两张线稿在文字之前占位，所以记号会绕开她们，不会压在脸上。

**三级层次，字号由「目标渲染宽度」反解，不是直接指定。** 这是词云研究的核心结论：字号作为编码只能分辨大/中/小三档，再细就是噪音；而统一字号会让长字符串仅仅因为长就显得重要。反解宽度可以配平视觉重量——当前锚点渲染宽 251–332，而那句 60 字德文宽 384 但字号只有 17，重量（宽×字号）只有锚点的三分之一。**改这套参数时看的是宽×字号，不是宽。**

**任何符号或文本不得出现两次**，包括不得与本扇区在圆环上的主记号重复（所以 `⊨` 不在 Scholarly 的纹理层里）。

> 🔴 **画板必须用 `preserveAspectRatio="xMidYMid meet"`，绝不能用 `slice`。**
> `slice` 会把画板缩放到**覆盖**视口：2000×2000 的画板在 2000×1013 的屏幕上，垂直方向只剩中间 ±506 单位可见。而 Scholarly 扇区朝上，内容集中在画板上半部——**结和一半符号直接渲染到了屏幕上方之外**，页面上只看得见结底部的 "Vie" 和 "Réel"。`meet` 保证整块画板可见，光束是独立元素、照样铺满屏幕。
>
> 由此还有两条：
> - **没有外圈半径上限。** 画板是完整可见的正方形，四角在半径 1414 处；用圆形上限会白白丢掉每个扇区最宽敞的部分。改用画板矩形边界判断。
> - **`R_MIN = 580` 是为了避开圆盘。** 画板按 min(vw,vh) 缩放，圆盘按 min(56vh, 30rem) 缩放，两者比例不固定；把视口范围代进去，圆盘外刻度最远到 549 画板单位（短屏最糟，此时圆盘吃掉一半以上画板半径）。

> 🔴 **验证必须检查「可见」，不是「存在于 DOM」。** 上面那个 bug 之所以躲过检查，是因为我只数了 DOM 节点和两两重叠——离屏元素照样有节点、照样有 bounding rect、照样报零重叠。**必须逐项断言 `getBoundingClientRect()` 完全落在视口内**，并且至少在 1280×720 / 1512×860 / 2560×1080 三个视口各测一遍。
>
> 另外两个已知的测量陷阱：
> - `getBBox()` 对变换组内的子元素返回**局部坐标**。拿它和画板坐标里的元素比较，等于比两套坐标系，永远报不出重叠。一律用 `getBoundingClientRect()`。
> - 首页页眉页脚是**全宽绝对定位盒子**，直接拿容器做碰撞检测会报大批假阳性。要对 `header h1 / header p / footer a` 这些真实文字元素测。

> ⚠️ **排版字宽和碰撞字宽必须是两张表。** `ADVANCE` 是典型字宽、用来反解字号（取太保守会让所有字都偏小）；`BOX_ADVANCE` 是最坏情况、用来算碰撞盒（**绝不能低估**）。等宽体这一档差距很大：JetBrains Mono 没有希腊字母和逻辑记号，`Σ Λ Φ ⊨` 全部走回退字体、实际约 0.78em，而等宽网格是 0.62em。两处都用 0.62 会让 `Σ = ΛΦΛ′ + Θ` 压进邻居 6 个单位。

**扇区点亮后内容错落显现**：每个元素带自己的 `transition-delay`（锚点 0–160ms、支撑 130–330ms、纹理 280–520ms），所以是逐渐聚拢而不是整块开灯。**关闭时延迟归零**——错开的退场会被读成卡顿。

> ✅ **当前的字号档位（上限 32）与错落时序已由所有者确认「很不错」，不要再动。** 加新素材时沿用现有分级，不要重新调参。

> 🔴 **符号层在手机上不渲染**（`@media (max-width: 767px), (max-height: 600px)` 下 `.symbol-field { display: none }`，2026-08-17）。
>
> 两个独立的原因，任何一个都足够：
>
> 1. **它压到圆盘上了。** 画板用 `meet`，会按视口的**短边**缩放。竖屏手机的短边是宽度：375×812 下画板渲染成 375×375，只占高度的 46%，而 `R_MIN = 580` 换算后落在离圆心 **109px**，圆盘外弧却到 **141px**。R_MIN 是按「圆盘被 56vh 限制、占画板 56%」推出来的；手机上圆盘改由**宽度**限制、占到 87%，比例整个反过来。
> 2. **就算不压，也读不了。** 画板缩放 0.1875 对桌面的 0.36，最小一档大约 **3px 高**。D13 花了很大力气让那一档在桌面尺度可读，而它在手机尺度上怎么努力都不可读。
>
> 两个条件都要：宽度管竖屏手机，高度管横屏手机（600px 这个阈值和 `.hide-when-short` 一致）。已验证 768×1024 仍然显示且仍然让开圆盘（R_MIN 223px 对外弧 206px）。
>
> **想在手机上恢复符号层，不要直接去掉这条规则**——那只会把上面两个问题原样放回来。要做就得给竖屏一套自己的布局（比如只留锚点层、放大若干倍），那是一个独立的设计任务。

**Scholarly 扇区里的 Borromean 结**（`borromean-knot.tsx`）是手绘 SVG，不是描位图——那个尺度下描图会糊，且缩放失真。它是该扇区唯一的拉康内容，代替整套数学素。

> ⚠️ **穿插方向不是装饰。** 三环成为波罗米环的充要条件是：**每一对之间，同一个环在两个交点上都在上方**。这样剪断任意一环，另外两环直接散开——因为它们本来就没互锁。若在一对内部交替上下，得到的是 Hopf 链，是另一个东西、另一套论证。当前实现是 Imaginaire 压 Symbolique、Symbolique 压 Réel、Réel 压 Imaginaire，所以每个环只在「循环中隔两位」的那个环处断开。**验证方法**：三条路径必须各含恰好 2 段弧且长度相等（当前均为 516）。
>
> 它值得占大位置不只因为内容：**这个结的拓扑和站点本身是同一个**——三样东西只有作为三样才成立。

Professional 扇区用**真实法规条号**而非抽象流程图形：`29 CFR 1910.147` 是他真正写过程序的联邦 lockout/tagout 标准。**可查证**是关键——内行一眼认出，外行看到的是纹理，比自造图形有分量。

`SymbolField` 对没有任何内容的扇区返回 `null`，不渲染空的遮罩 SVG。

**验证方法**：遍历渲染后的 `getBBox()` 两两求交，以及检查每个盒子四角的极角是否在扇区内。`getBBox()` 返回的是 viewBox 坐标，**所以这个结论天然与分辨率无关**。

**2026-08-09 实测**（此前记的「64 / 46 / 34」是旧内容留下的，已过期）：

| 扇区 | `content.ts` 条目 | 实际落位 | 重叠 | 出界 |
|---|---|---|---|---|
| Scholarly | 3 + 12 + 30 = 45 | 45（另加结自带的 10 个标签，DOM 里共 55 个 `<text>`） | 0 | 0 |
| Creative | 0（**故意留空**，见 `SYMBOL_LAYERS.creative` 的注释，只有两张线稿） | 0 | 0 | 0 |
| Professional | 1 + 5 + 9 = 15 | 15 | 0 | 0 |

**一个都没被丢弃**。数这个数的时候注意：Scholarly 的 SVG 里有 55 个 `<text>`，其中 10 个是波罗米环自己的标签（Imaginaire / Symbolique / Réel / Corps / Mort / Vie / Sens / JA / JΦ / a），不是纹理层的。直接数 `<text>` 会多出 10。

### D14 — DEAR SUSPECT 线条版 ✅ 生效中

`public/creative/dear-suspect-figure.png`，由 `scripts/keyart-lineart.py` 从工作室 press kit 的 `key_art_main.png` 派生。源图不入库（在 OneDrive 里），脚本入库。

**不要用边缘检测重做这张图。** 原画本身就是笔触，边缘算子会在每一笔周围描出双线并丢掉墨色轻重。正确做法是**把黑底键掉、保留笔触**：以 `max(R,G,B)` 作为"有墨"的度量（红色笔触的亮度只有 0.1 会被吃掉，但最大通道有 0.63），映射到 alpha，再给四边羽化——原图在自己的下边缘把裙摆切断了，不羽化会显得像失误。

### D15 — 布局数值吸附到网格，而不是换成整数哈希 ✅ 生效中（2026-08-09）

**背景**：`SymbolField` 报 hydration mismatch，React 放弃整棵树的 hydration。原因不是「用了 `Math.random`」——代码本来就避开了它——而是 **`Math.sin` 在 ECMA-262 里是实现自定义的**。`rand()` 用的 `sin(seed) * 43758.5453` 是着色器圈的老写法，纯函数没错，但**纯 ≠ 跨引擎一致**：Node 和 Chrome 的 `Math.sin` 差最后一个 ulp，43758 倍的乘数把它放大到单位区间的 ~1e-12，`angle` 和 `radius` 再放大到画板单位的 ~1e-8——正好是 React 报出来的 `x` 属性差异量级。

实测（同一份代码，5000 次求和）：

| | Node v24.14.1 | Chrome |
|---|---|---|
| `sin` 版 | `2475.354464846803` | `2475.354464846786` |
| 整数哈希版 | `2511.84365374105982482` | 完全相同 |

**决策**：`rand()` 的输出吸附到 1e-6 网格，`cx` / `cy` 吸附到 1e-3 网格（且**在建碰撞盒之前**吸附，让碰撞检测和渲染用同一个数）。

**为什么不换成整数哈希**（murmur3 finalizer 那种，构造上就 bit-exact）：**它会把每个符号的位置全部重排**，也会改变哪些条目因放不下而被丢弃。D13 的构图是已经验证过的，本次任务是修动画，不是重做符号层。**这是为了保住构图而做的取舍，不是没想到更彻底的办法。**

**为什么网格是 1e-6 而不是 1e-9**：布局过程大约要抽一千次随机数。1e-9 网格下，单次抽样跨过舍入边界的概率约 2e-3，一千次就有约 2 次——不可忽略。1e-6 比分歧量（1e-12）粗六个数量级，比可见位置细九个数量级。

**代价**：`Math.cos` / `Math.sin` / `Math.atan2` / `Math.hypot` 仍留在管线里，理论上仍可能在 1 ulp 处翻转某个边界判断。1e-3 的吸附把这个概率压到约 1e-10/坐标，但**不是零**。真要彻底根除，只能把布局改成服务端算一次、序列化进 HTML 传给客户端（约多 15KB 载荷，且要一路 prop drilling）。当前不值这个价。

> ⚠️ **给未来的 AI**：不要以为「没用 `Math.random` 就 hydration 安全」。任何超越函数（`sin` / `cos` / `tan` / `atan2` / `hypot` / `exp` / `pow` 的非整数次幂）在 JS 里都是实现自定义的，服务端和浏览器可以给出不同的最后一位。**只要结果会被渲染成属性，就必须吸附到网格。**

### D16 — 中英双语，英文规范、中文增量翻译 ✅ 生效中（2026-08-09）

**决策**：站点改为 `/[lang]/…` 双语（`en` / `zh`），推翻 §4 的「一律英文」。

**路由**：`src/proxy.ts`（**Next 16 把 `middleware.ts` 改名为 `proxy.ts`，导出名也从 `middleware` 变成 `proxy`——不要改回去**）。`app/[lang]/layout.tsx` 就是根布局，`app/layout.tsx` 已不存在。两个 locale 都在 `generateStaticParams` 里，所以 12 个页面全部静态预渲染，proxy 只负责重定向。

**「跟随浏览器语言」必须配一条例外**：只有在**没有显式选择**时才读 `Accept-Language`。用户手动切过语言就写 cookie，之后永不自动跳。理由：无条件按 header 跳转会把明确想看另一种语言的人每次都弹走，也会把爬虫弹走、导致只有一种语言被收录。访问带 locale 前缀的地址同样会写 cookie——顺着别人分享的 `/zh/...` 进来，之后就留在中文。

**翻译可以是子集。** `getDictionary()` 把中文词典**浅合并**到英文之上，缺的键回落英文而不是渲染空白。这就是敢在只译完首页时上线的原因。故意只合并一层：再往下每个值都是一个完整单元（三条线、整个 hub），半译一个是错误不是特性。

> ⚠️ `src/lib/i18n.ts` **只能被服务端组件引用**。两份词典都是静态 import，被客户端组件拉进去就会把全部译文打进浏览器包。`HomeStage` / `TrinityDisc` / `LanguageToggle` 都是客户端组件，一律**以 props 接收字符串**，只 `import type`。

### D17 — 中文的字体与「斜体」 ✅ 生效中（2026-08-09）

**D8 的三套拉丁字体一个中文字都没有。** 不补中文字体的话，每个汉字都会回退到系统字体（Windows 微软雅黑 / macOS 苹方 / Linux 随缘），D8 的排版对撞在中文版里直接塌成通用 UI 黑体——和 D9 记录的记号回退是同一个病。

**决策**：字体栈写成「拉丁在前、思源在后」，浏览器**按字符**选族，一套栈同时管两种文字，普通正文不需要 `:lang()` 切换。

| 角色 | 拉丁 | 中文 |
|---|---|---|
| display | Cormorant Garamond | 思源宋体 Noto Serif SC |
| sans | Space Grotesk | 思源黑体 Noto Sans SC |
| mono | JetBrains Mono | **不配**——中文没有等宽传统，硬凑更糟；栈里的思源只为避免豆腐块 |

> 🔴 **CJK 字体必须 `preload: false`。** Google 把中文切成上百个 unicode-range 分片，`preload` 默认 `true` 会给**每一个分片**注入 `<link rel=preload>`。实测：加了 `preload: false` 后页面只有 4 个 preload（全是拉丁）；不加会是七百个左右，而且恰好废掉分片按需加载这个机制本身。
>
> 已验证：`Noto Sans SC` 注册 303 个 face、实际只下载 24 个；`Noto Serif SC` 注册 404、下载 3 个。**代价是样式表里有 775 条 `@font-face` 规则**——只是元数据不是字体二进制，但不是零成本。

**中文没有斜体。** 任何汉字字体都不存在 italic 字面，思源没有、以后也不会有，因为这个概念不在传统里。强行 `font-style: italic`，浏览器只能把字形整体切变约 12°，笔画粗细全乱，读起来像渲染故障而不是强调。

中文里承担 italic 职能的是**换一种字体**。选**仿宋**不选楷体：仿宋清瘦峻峭，楷体温润，后者会把调子拉暖、和 §4 要的高对比打架。

实现是 `globals.css` 里的 `:lang(zh) .italic` 规则——`font-style: normal` + 换族。英文侧照常用 Cormorant 的真斜体，中文里夹的拉丁片段也照常斜，因为族栈先命中 Cormorant。

> ✅ **已完成（2026-08-09）**：[朱雀仿宋](https://github.com/TrionesType/zhuque) v0.212（OFL 1.1）已子集化后 vendore 进仓库，`next/font/local` 加载。**Google Fonts 上没有任何仿宋**，别去那里找。
>
> `scripts/subset-fangsong.py` 从上游 8.42 MB 的 ttf 里只切出站点实际用到的字符——当前 132 字、**37.3 KB**。CJK 整字体不可能直接上线，而常见的 unicode-range 分片方案是给「文案未知」的站点用的；本站所有中文都在 `src/lib/i18n.ts` 里，字符集在构建期就是已知的，所以单个小文件是更对的答案。**加了中文文案就要重跑这个脚本**，脚本会断言每个请求的字符都在输出里。
>
> 上游 ttf **不入库**（5.5 MB 构建输入），许可证随子集走：`src/app/fonts/OFL.txt`。
>
> ⚠️ **不要把 `next/font/local` 的变量命名为 `fangsong`。** next/font 用变量名生成字体族名，而 `fangsong` 是 CSS Fonts 4 里的**通用族关键字**（与 `serif`、`monospace` 同级）。不带引号出现在 `font-family` 里会被解析成通用关键字，`@font-face` 根本不被查询——字体照常加载、字体栈看起来也对，浏览器却在画系统字体。这个坑第一次就踩了。
>
> ⚠️ **验证中文字体不能用字宽。** CJK 字形一律 1em 全宽，任何字体在同一字号下 `measureText` 都返回相同数值——这条对拉丁字符有效的检测方法（见 D9）在中文上完全失效。要**光栅化到 canvas 后比对像素**：把同一个字分别用目标族和 `serif` 画出来，取位置加权的强度和，不同才说明字体真的生效了。

### D18 — 上线元数据：绝对 URL、逐页 hreflang、预览不收录 ✅ 生效中（2026-08-12）

**决策**：补齐 `metadataBase` / canonical / hreflang / Open Graph / `robots.txt` / `sitemap.xml`，并生成品牌图（favicon、apple-icon、OG 卡片）。站点地址由 `src/lib/site.ts` 统一解析。

**地址解析顺序**：`NEXT_PUBLIC_SITE_URL` → `VERCEL_PROJECT_PRODUCTION_URL` → `localhost:3000`。

**故意不读 `VERCEL_URL`**（那是单次部署的地址）。预览构建也要报出**生产**域名，否则每个预览部署都会把自己声明成 canonical，等于自己和自己抢排名。

**代价**：本地 `npm run build` 出来的绝对 URL 全是 `localhost:3000`。这是对的（本地就是那个地址），但看构建产物时别误以为是 bug。

> 🔴 **canonical 和 hreflang 必须逐页声明，不能写在 layout 里。**
> 元数据是**继承**的：在 `[lang]/layout.tsx` 上写 `canonical: "/en"`，`/en/cv` 和 `/en/scholarly` 会一起继承它——等于告诉爬虫这些页面都是首页的副本，应当从索引里删掉。同理，layout 级的 hreflang 会让 `/en/scholarly` 指向 `/zh` 而不是 `/zh/scholarly`。
>
> **这个 bug 在补元数据之前就已经存在**（原 layout 里那段 `languages: { en: "/en", "zh-Hans": "/zh" }`），只在首页是对的。现在由 `localeAlternates(lang, path)` 逐页生成，六个页面各自调用。

> 🔴 **`openGraph` 在路由段之间是「替换」不是「合并」。**
> 把 `opengraph-image.png` 放在 `src/app/` 根段、同时在 `[lang]/layout.tsx` 里声明 `openGraph`，结果是**所有页面都没有 `og:image`**——深层段的 `openGraph` 整个盖掉了根段贡献的 `images`。症状很隐蔽：`og:title`、`og:description` 都在，只有图没了，而 `/_not-found`（在根段）反倒有图。
>
> 把文件移进 `[lang]/` 能让合并生效，但**换来一个更糟的 URL**：动态段下的图片路由没有自己的 `generateStaticParams`，Next 用占位符填参数，emit 出 `https://host/-/opengraph-image.png`，指向不存在的地址。
>
> 现行解法：文件留在根段（以 `/opengraph-image.png` 无参数提供），在 layout 里**手写 `images: [OG_IMAGE]`**。相对路径会按 `metadataBase` 解析成绝对地址。**移动这个文件不会改变实际生效的图**，改路径要同时改 layout。

**预览部署一律 noindex**：`INDEXABLE = process.env.VERCEL_ENV !== "preview"`，同时作用于 `robots.txt`（`Disallow: /`）和每页的 `<meta name="robots">`。两层都要，因为 `robots.txt` 挡的是抓取、`meta` 挡的是「别人直接贴了预览链接」导致的收录。非 Vercel 环境（本地、CI）按可收录处理，这样本地构建出来的就是生产会有的东西，而不是一个永远没人检查的特例。

**sitemap 不写 `lastModified`**：构建期唯一能拿到的日期是构建时间本身，那会把 12 个页面每次部署都标成「刚改过」。Google 认得出不可信的 lastmod 并直接忽略——不写比编一个更有价值。

**品牌图由 `scripts/brand-images.py` 生成**，几何直接取自 `trinity-disc.tsx`（角度、半径比例、骨白描边、刻度环）。

- **favicon 是终曲记号，不是圆盘**（2026-08-15 由所有者定，见 D21）。~~圆盘版曾把三色提亮以便在 16px 下still分辨~~ —— 已废弃，那套提亮色值不再使用。OG 卡片仍是圆盘：保留真值颜料、骨白描边、刻度环和头像，因为它只会被大图看到。
- **卡片的骨白描边比页面重**（1.7 单位 / alpha 140，页面是 1.1 / 0.45）。链接预览经常只渲染到三分之一宽，页面那个权重在那里直接消失、圆环塌成饼图。实测：按页面数值时外弧边缘像素只抬到 `(60,97,192)`。
- 字体从 `.next` 里 next/font 产出的 woff2 捞（fontTools 转 ttf 供 Pillow 用），所以卡片是站点真正的字面，不是 Georgia 之类的替身。

> ⚠️ **从 `.next` 里找字体不能只匹配家族名。** next/font 按**用量**切分，不是按字系切分：排序后第一个 JetBrains Mono 文件只有 **10 个字形**，含 `A` 不含 `S`。只判断家族名（甚至只判断「有没有 A」）会选中它，卡片上就是一排豆腐块。正确做法是**要求该文件覆盖即将绘制的每一个字符**，并在符合的文件里取 cmap 最大的那个。

> ⚠️ **Pillow 的 `ImageDraw` 是「写像素」不是「合成」。** 直接在 RGBA 图上画半透明骨白，会把底下的颜料**抠掉**换成半透明骨白，看起来是一条黑边而不是细线。所有半透明笔画先画在独立图层上，最后 `alpha_composite` 上去。合成放在最后一步，这样内弧描边压在 void 圆上、中心环压在头像上，和页面的叠放次序一致。

**`proxy.ts` 的 matcher 排除了带点的路径**，所以 `/robots.txt`、`/sitemap.xml`、`/icon.png`、`/opengraph-image.png` 不会被加上语言前缀。**改 matcher 时要重新确认这一点。**

### D19 — 圆环上标签的半径是解出来的，不是定出来的 🚫 已废弃（2026-08-19，求解器已删除）

> 🔴 **求解器（`useBalancedLabelRadius`）已随 D27 一起删掉，标签半径回到一个常数。**
>
> 这不是走回头路。这条决策存在的前提是「标签必须塞进一条 100 单位厚的圆环带」——`PROFESSIONAL` 在任何半径上都塞不进去，所以才需要解一个内外余量相等的平衡点。**Venn 没有带，只有月牙形（lune），那里空间大得多**：现在的常数把标签放在自己那个圆的独占月牙里，离自己的圆心 53 单位、离另外两个圆心 166 单位（圆直径 118），三边都宽裕。约束消失了，求解器就成了一个解决不存在的问题的复杂装置。
>
> 下面保留原文，因为它解释了为什么「一个常数服务不了两种语言」在**圆环**几何下是真的——如果哪天又回到带状布局，这套推理仍然成立。同时它也记录了两个仍然有效的教训：**量墨迹不要量外接盒**（四角是空的），以及 **`ResizeObserver` 在预览面板里不触发**。


**背景**：所有者反馈「学术和学术符号太靠下、创作和 Coda 符号太靠右，都不够对称」。这**是同一个缺陷的两个视角**——三个标签块统统被拉得离圆心太近。朝上的扇区里「太靠内」看起来就是「太靠下」，左下的扇区里就是「太靠右」。

**旧实现**：`R_LABEL = 99` 一个写死的常数。它有两个问题。

1. **一个常数服务不了两种语言。** 圆盘按视口缩放而字号不缩放，所以标签占画板的比例随窗口变；中文标签两个字、英文 `PROFESSIONAL` 十二个字母，宽度差 4 倍。实测最优半径：**英文短窗口要 108，中文高窗口要 122**。中间没有共同解。
2. **它是照着错误的形状调的。** 当初用的是标签块的**外接盒**，而块里是「记号 + 说明」两行居中排布，四个角是空的。拿空角去卡外弧，结果是把真正的字一路推进中心圆：1280×600 下 Creative 的字已经压进圆心区 **10 个单位**。

**现在**：`useBalancedLabelRadius` 在挂载时量一次，解出让**内外余量相等**的那个半径。

- 量的是**两个子元素（记号、说明）各自的盒**，不是它们的容器——空角不再参与计算。
- 墨迹角点先换算成**相对该块自身中心**的偏移，这样它们与半径无关，半径可以直接**二分求解**，不需要「挪一点再量一次」地迭代。
- 二分区间就是 `[R_INNER, R_OUTER]`，所以**再离谱的测量结果也只能把标签放在圆环内**，不可能甩出圆盘。
- `document.fonts.ready` 后**重新量一遍**。Cormorant 比回退衬线宽出不少，只在挂载时量一次等于按访客根本看不到的字体排版。

**三个标签共用一个半径，不是各算各的。** 等半径才是旋转对称，而旋转对称正是「对称」在这个构图里的定义。各自最优会让三个块坐在肉眼可见的不同深度上——那恰好是所有者抱怨的东西。

**实测结果**（内 / 外余量，单位为画板单位）：

| | 视口 | 圆盘 | 解出的 R | 内 | 外 |
|---|---|---|---|---|---|
| 中文 | 1512×944 | 480px | 122.5 | 23.1 | 23.1 |
| 中文 | 1366×620 | 347px | 122.1 | 13.0 | 12.9 |
| 英文 | 1512×944 | 480px | 113.1 | 13.7 | 13.7 |
| 英文 | 1280×600 | 336px | 108.7 | −1.2 | −1.2 |

最后一行两边都是 −1.2：**该视口下英文标签确实塞不进 100 单位厚的圆环**（需要 102.4）。这时「平衡」是唯一还有意义的目标，而 1.2 单位约合 1 像素，看不出来。对照旧值：同样视口下旧实现是内 −10.4 / 外 8.5。

**代价**：`TrinityDisc` 多了一次客户端测量。首屏用 `R_LABEL_START = 112` 渲染（服务端和客户端首帧一致，不会 hydration mismatch），随即在 `useLayoutEffect` 里改写——标签要到 1250ms 才淡入，所以这次改写永远不会被看见。

> ⚠️ **`useLayoutEffect` 必须包一层 `typeof window === "undefined" ? useEffect : useLayoutEffect`。** 这是客户端组件，Next 仍然会在服务端渲染它，直接用 `useLayoutEffect` 会在构建和 SSR 日志里刷警告。但也**不能干脆换成 `useEffect`**：`prefers-reduced-motion` 生效时淡入被压到 0.01ms，标签立刻可见，`useEffect` 的修正就会变成一次肉眼可见的跳动。

> 🔴 **`ResizeObserver` 在 Claude 的预览浏览器面板里根本不触发，`window.resize` 也不触发。** 实测：把画板从 480px 强制改成 300px，布局确实重排了（量出来的墨迹坐标全变了），但 RO 计数器始终是 0。原因是 RO 的回调派发挂在渲染生命周期上，而面板没显示时页面不合成帧——同一个原因也让 `computer{action:"screenshot"}` 直接超时。
>
> **所以「改了视口 R 没变」在这个环境里是工具的假象，不是 bug。** 要在这里验证不同视口，得**先改视口再重新导航**，靠挂载时那次同步测量出结果。别为了这个假象去改代码。

### D20 — 根域名是主，www 是 308 重定向 ✅ 生效中（2026-08-15）

**决策**：`yixuancodashi.com` 是唯一正式地址；`www.yixuancodashi.com` 以 **308 永久重定向**指向它。

> 🔴 **不要接受 Vercel 添加域名弹窗里那个默认勾上的「Redirect apex domains to www (recommended)」。** 它是反的。
>
> 勾上之后根域名会跳去 www，而 `NEXT_PUBLIC_SITE_URL`、canonical、og:image、sitemap 全部写的是**根域名**。结果是「canonical 指向一个自己会跳走的地址」——自相矛盾的信号，搜索引擎多半改用 www，于是上述四处**全部作废、要推倒重设**。
>
> Vercel 标 "recommended" 有历史原因（根域名不能用 CNAME、根域名的 cookie 会漏给所有子域名），**这两条对静态个人站都不成立**。
>
> 它还会连带报一个看不懂的错：`You have redirected another domain (…vercel.app) to this domain. In turn, you cannot redirect this one.` 真正的原因是 `vercel.app → 根域名 → www` 构成了重定向链，而 Vercel 不允许链。**取消那个勾，错误自己消失。**

**www 用 308 不用 307**：307 是临时，等于让搜索引擎两个地址都收录；308 是永久，把权重并给根域名。Vercel 弹窗默认给的是 307，要手动改。

**绑定路线**：NS 留在 Porkbun，只在 DNS 里指记录。根域名 `A → 216.198.79.1`，`www` `CNAME → cname.vercel-dns.com`。

> ⚠️ **Vercel 那一页上「Nameservers」和「CNAME 记录」两块挨得很近，别抄错。** 曾把 www 的 CNAME 填成 `ns1.vercel-dns.com`——那是**域名服务器**（解析到 `198.51.44.13`，只跑 DNS 不跑 HTTP），属于「把整个域名托管给 Vercel」那条**互斥**路线。症状：明文 HTTP 有 `Server: Vercel` 的 308，HTTPS 却握手失败。

> ⚠️ **证书签不出来，先查该域名在不在 Vercel 项目的 Domains 列表里，不要干等。** 曾经 DNS 全对、等了 5 分钟仍 `SEC_E_WRONG_PRINCIPAL`，原因是 `www` 根本没加进项目——Vercel 只给登记过的域名签证书。**决定性的判据是看证书的 SAN**：
>
> ```bash
> openssl s_client -connect yixuancodashi.com:443 -servername yixuancodashi.com </dev/null 2>/dev/null \
>   | openssl x509 -noout -text | grep -A3 "Subject Alternative Name"
> ```
>
> 只列出根域名就说明 www 没登记，等多久都不会有。另外：**项目级 Domains 页在 `/<team>/<project>/settings/domains`**，账号级的 `/<team>/~/domains` 只管「你名下有哪些域名」，加不了子域名。

**Porkbun 的默认记录必须清掉**：新域名自带 `ALIAS @ → uixie.porkbun.com` 和 `CNAME * → uixie.porkbun.com`（停放页），还有 **URL Forwarding**，三者都要清。**保留** MX ×2 + SPF TXT（免费邮件转发，以后要 `hi@yixuancodashi.com` 用得上）和 `_acme-challenge` TXT（Porkbun 自己的证书校验，无害）。

**Porkbun 的 Host 栏留空代表根域名**，不要填 `@`、更不要填完整域名——它会自动补后缀，填 `yixuancodashi.com` 会变成 `yixuancodashi.com.yixuancodashi.com`。

> ℹ️ Vercel 面板对 www 显示 **DNS Change Recommended**，建议改成项目专属的 `63eddaae7969fbe8.vercel-dns-017.com`。**这是建议不是错误**，Vercel 自己的说明写着 `cname.vercel-dns.com` 与 `76.76.21.21` 会继续可用。改了能消掉那个黄色感叹号。
### D21 — 首页英文改走古典；站点 LOGO 定为终曲记号 ✅ 生效中（2026-08-15）

**由所有者提出**：「主界面的 profile 还有按钮字体都古典一点，不要太现代，主要是针对英文。中文没事。」

**根因**：D8 的三体对撞**在两种文字里不对称**。Space Grotesk 与 JetBrains Mono 都带着当代／技术的调性，而**这个调性只存在于拉丁文**——中文侧同一套字体栈落到思源，读起来是中性的。于是同一块屏幕，英文版明显比中文版现代。

**做法**：`.home-stage` 作用域下，`.label` 改用 `--font-display`，13px / 字重 600 / 0.18em；简介段落（`.intro-copy`）另外改，**仅限英文**。

> ⚠️ **`.label` 这一档最初只作用于 `:lang(en)`，2026-08-16 改成两种语言都作用。**
> 原来的理由是「中文侧落到思源、本来就中性」——但那是在推理**汉字**，而页脚大半不是汉字。`诗文` 是衬线，紧挨着的 `ELEGISTS STUDIO`、`GITHUB` 却是 JetBrains Mono，并排读起来像两个网站。所有者原话：「中文版左下角的这一部分字体你没有修好啊」。
> 中文侧顺着同一套栈落到思源宋（Song 面），正是中文里对应的古典档位。**简介段落仍只改英文**——所有者没提它，而它是正文不是标签。

> 🔴 **绝对不要把这套改成全局 `.label`。** D12 依赖 `.label` 保持等宽：`/cv` 上「等宽的表格数字」与「Cormorant 的老式数字」并置，同一个年份读作两个文明——两边都变成 Cormorant，这个对撞就没了。所以作用域挂在 `HomeStage` 的 `.home-stage` 上，`SiteShell` 那条线的页面一律不受影响。

**Cormorant 在小字号偏纤细**（D8 早就记了），所以字号从 11px→13px、圆盘标签 10.5px→12px、简介 12/14px→16/18px，字重提到 600。大写 Cormorant 配 0.18em 字距读起来像碑刻，而不是像小字注释。

圆盘标签的字号走 `var(--disc-label-size, 10.5px)`：内联样式必须赢过 `.label` 的字号（原因见组件里的注释），而内联又没法写媒体查询或 `:lang()`，用变量是唯一不打架的办法。**改完 D19 的求解器会自己重新配平**，实测 R 从 113.1 → 108.4，内外余量 4.0 / 4.0。

**LOGO 换成终曲记号**：`icon.png` / `apple-icon.png` 不再是三扇区圆盘，改为 `𝄌`，几何与 `track-mark.tsx` 同一套 24 单位网格。骨白配虚空，**不带任何一种轨道颜料**——这个记号代表 Coda 本人而非三条线中的任何一条，与 D9「中心不编码」一致。

> ⚠️ **图标里的十字笔画要比页面上重。** 页面用 1.25 单位，换算到 16px 的标签页图标只有 0.8px，降采样后基本消失——**而没有十字的终曲记号只是一个圈**。图标用 2.1 单位。已在 16/24/32/48/64px 逐档看过。

### D22 — Verse & Prose 是独立 section，且不走 CV 那套 i18n ✅ 生效中（2026-08-16）

**决策**：新增 `/[lang]/writing`（索引）与 `/[lang]/writing/[slug]`（单篇）。英文名 **Verse & Prose** 由所有者定，中文「诗文」。

> 🔴 **诗文的 i18n 模型和站点其余部分是相反的，不要「统一」它。**
>
> 别处是「英文规范 + 中文覆盖 + 缺失回退英文」（D16），这对简历条目正确：同一件事，两种说法。**对原创作品是错的**——那首诗originally就是中文写的，不是任何东西的译文；一首诗的英译是**另一个作品**，不是缺失的字段。套用回退逻辑，只有中文的诗会在 `/en` 上显示成「译文忘了填」，或者干脆开天窗。
>
> 所以 `Piece.text` 是 `Partial<Record<Locale, PieceText>>`：每篇声明自己**存在于**哪些语言。索引只列当前语言真实存在的篇目；只存在于另一种语言的篇目**照样列出、照样可点**，标注为「英文原作 / Chinese original」——藏起来会低报作品数量，硬翻会谎报作品性质。

**措辞是肯定式，不是缺失式。** 标注写「英文原作」而不是「暂无中文版」；这些是原作，没有任何东西缺失。

**无题作品不编标题。** `title` 可省略，索引用**首行**指代（这是无题诗的通行做法），单篇页则**不渲染标题**——把首行当标题印一遍、正文再印一遍，读起来像 bug。替所有者取标题是代笔，CLAUDE.md §4 已经禁止。slug 是 URL 标识不是标题，必须是拉丁字符且稳定，所以由首行意译而来，**分享出去之前可以随便改，之后不能**。

**正文是 `string[]`，不是 Markdown / MDX。** 诗在哪里断行**本身就是作品**，交给解析器等于把唯一不能靠猜的东西交出去；顺带省掉一个构建依赖和一条新的 hydration 风险路径（D15）。空字符串是分节。

**字体由「作品的语言」决定，不是「页面的语言」。** 中文诗在 `/en` 上依然是中文，依然要仿宋，所以 `PieceBody` 自己写 `lang` 属性并显式指定 `--font-fangsong`——`:lang(zh)` 那条规则只改斜体，不指定就会继承 `--font-display` 落到思源宋（Song 面）。

**两个入口**：`/coda` 那一列改成「最新一篇全文 + 全部诗文 →」；首页放在**页脚最左**，用竖线隔开、Cormorant 斜体而非 `.label`。

> ⚠️ **首页入口不要放到圆盘上。** 圆环编码三个**社会身份**，D9 要求私我不被编码，而诗文是私我最深的一层——加第四个扇区或第四个记号，等于把「独立于社会之外」这句话作废。选页脚还有个硬理由：**页眉的简介和提示语在扇区点亮时会淡出，页脚不会**，入口不该在用户探索时消失。

**子分类不开独立路由**，在索引页按体裁分组。现在只有一篇，`/writing/verse` 和 `/writing/prose` 会是两个近乎空的页面，而空页面读作疏于打理而非克制。某一类超过约十篇再拆。**空的体裁不打印标题。**

**sitemap 里每篇都出双语两条**（`src/app/sitemap.ts`）。只有中文的篇目在 `/en` 下依然有真实可访问的 URL（原文照登 + 标注），所以两条都成立；**只登记「写作语言」那一条会留下半个 hreflang 簇，Google 会整簇丢弃**。

> ⚠️ `localeAlternates()` 的 `path` 参数已从 `ROUTES` 的联合类型放宽为 `string`——`/writing/[slug]` 是动态的，没法枚举进类型。

> ⚠️ **加中文文案后必须重跑 `scripts/subset-fangsong.py <上游ttf>`。** 本轮从 734 字涨到 738 字、230 KB。
>
> **上游 ttf 的取得方式**（它不入库，见 D17；`.gitignore` 已封 `ZhuqueFangsong-*.{zip,ttf}`）：
>
> ```bash
> curl -sSL -o ZhuqueFangsong-v0.212.zip https://github.com/TrionesType/zhuque/releases/download/v0.212/ZhuqueFangsong-v0.212.zip
> ```
>
> 解压出 `ZhuqueFangsong-Regular.ttf`（8.4 MB）放仓库根目录即可。机器 A 上已备好。
>
> 🔴 **绝不要因为某个字不在子集里就改文案。** 本轮一度这么做过，被所有者当场否掉——**「别改我的字」**。字体是为文案服务的，不是反过来。缺字就去把上游 ttf 弄回来重切，这是唯一正确的处理。
>
> 不需要上游 ttf 也能查缺字：读 `src/app/fonts/zhuque-fangsong-subset.woff2` 的 cmap，与 `src/lib/i18n.ts` + `src/content/writing/*.ts` 里所有 CJK 字符求差集。

### D23 — 触屏自演示；等宽退出界面；中文只用一套字 ✅ 生效中（2026-08-17）

三件事同一批做，都由所有者在手机上看到实际效果后提出。

#### 触屏上圆盘会自己演示一次

**问题**：圆盘的所有反应都绑在 hover 上，而手机没有 hover——**第一次点击就是导航的那次点击**。于是光束（整个构图围绕它建立，也是「点下去会去哪」的预览）在手机上**从来没有人见过**。

**没有采用**「第一下点亮、第二下跳转」。那会把日常动作的成本翻倍，去教一件访客没要求被教的事。

**做法**：入场结束后（约 1.9s）自动依次点亮三个扇区，然后归位；点击仍然一次跳转；**任何一次 `pointerdown` 立刻取消演示**，不和用户抢。

> ⚠️ **三步之间不能留空档。** 2100 / 2850 / 3600ms 点亮、4350ms 归位——中间没有 `null`，所以 `lit` 全程为真，左上角简介**只淡出一次、淡入一次**。中间插一个 null，简介会在两秒里闪三下。
>
> ⚠️ 判据用 `(hover: hover)` 而不是屏幕宽度：笔记本上的小窗口仍然有指针，大平板仍然没有。`prefers-reduced-motion` 下整个跳过。

> 🔴 **这个演示没法用常规办法在预览面板里验证。** 工具往返约 10 秒，而演示窗口在 2.1–4.35 秒——`navigate` 之后再执行脚本永远太晚（实测探针在页面加载后 10.4 秒才开始）。而且 `navigate` 到当前地址是空操作，页面根本不会重新挂载（实测 `performance.now()` 已经 54 秒）。
>
> **可行的办法**：先装好采样器，再用**客户端路由跳转**（点语言开关）让 `HomeStage` 重新挂载——整页不刷新，JS 上下文得以存活。实测拿到了完整时间线：`+2419ms [1,0,0]` → `+3013ms [0,1,0]` → `+3920ms [0,0,1]` → `+4510ms` 归位，简介 1→0→1 各一次。

#### 等宽体退出界面，只留给符号层

所有者指着页面上残余的 JetBrains Mono：「这个字体在中英文版都有出现，但是现在位置很少了，考虑用现有字体也一起替换掉？」

首页改古典之后，等宽只剩返回链接、几个区块标题这些零散位置，**读起来像上一版设计的遗留，而不是刻意的对撞**。现在 `.label` 全站改用 display 族。

**等宽没有被删掉，而是收窄到只服务符号层里的记号**（`Σ = ΛΦΛ′ + Θ`、样本量、法规条号）。这才是 D8 说的「一切属于数据的东西」——它现在是一种**记号**，不再是一种**界面声音**。因此 `--font-mono` 与 `next/font` 里的 JetBrains 都保留，不要删。

对 D12 的影响见该条。

#### 中文只用一套字

**问题**（所有者原话：「中文版本的字体选择不够统一，没有形成足够统一的视觉语言」）：中文侧标题和标签走思源宋、正文走思源黑，而**没有任何规则决定谁走哪个**。英文里这个分裂读作「古典对几何」，是 D8 的立意；中文里它只读作不一致。

**做法**：`:lang(zh) body { font-family: var(--font-display) }`。中文正文改宋体，**仿宋继续承担文学声部**（D17）。这正是传统的分工——正文用宋，另一种声音用仿宋——**三套字变成两套**。

> ⚠️ **英文侧不动，而且必须不动。** Cormorant 在 14px 正文尺度下太纤细（D8 早就写了），所以这条规则是 `:lang(zh)` 作用域的，不是全局的。实测：`/en/cv` 正文仍是 Space Grotesk 14px，`/zh/cv` 全页已无 JetBrains Mono。

### D24 — 手机上的三处修正；诗文的分类与体例 ✅ 生效中（2026-08-17）

#### 触屏：点击→展开→再跳转

> 🔴 **触屏必须挂 `pointerdown`，不能挂 `click`。这是「点两下才进去」那个 bug 的全部原因。**
>
> 扇区和标签都带 hover 反应。**iOS 上，带 hover 行为的元素会吞掉第一次点击**——那一次只应用 hover 态（所以光束确实亮了），`click` 要等到**第二次**才派发。任何挂在 click 上的逻辑，构造上就必然要点两下，跟它自己做什么无关。
>
> `pointerdown` 在第一次触摸就到，所以流程从那里起；随后可能到来的 click 用 `swallowClick` 吞掉，避免二次导航或截断光束。指针设备靠 `event.pointerType === "mouse"` 提前返回，走原来的 click 路径。
>
> 停留时长 **1000ms**（所有者要求「拉到一秒」），光束自身过渡 700ms，所以颜色铺满后还有一拍才换页。

D23 那个「入场后自动依次演示三个扇区」**被所有者否掉**（「不对不对，手机上不是逐个演示」）。现行：**点击某扇区 → 点亮 → 约 680ms 后跳转**。光束本身的过渡是 700ms，所以颜色铺满屏幕的那一刻，下一页（底色正是同一种颜料，D10）接上。指针设备不受影响——hover 已经跑过光束，那里再加延迟只会显得卡。

> ⚠️ 判据是 `(hover: hover)` 与 `prefers-reduced-motion`，二者任一成立就直接跳转。`leaving` ref 防止连点排队出多次 `router.push`。

#### 符号层回到手机上（D23 把它藏了，也被否掉）

**没有恢复成原样，而是把圆盘压小。** 竖屏下画板按宽度缩放，`R_MIN` 落在 `0.29·vw`，而圆盘外弧在 `0.43·宽度`——所以圆盘必须小于 `0.674·vw`。`max-w-[min(56vh,30rem,62vw)]` 里的 **62vw 就是干这个的**，实测 375 宽下圆盘 233px、外弧 100px、R_MIN 109px，让开 9px。

> 🔴 **改这个 max-w 之前先算一遍。** 62vw 看起来像个随手写的响应式断点，其实是符号层不压到圆盘上的唯一约束。桌面和横屏都由 `56vh`/`30rem` 先行封顶，这一项只在竖屏手机生效。
>
> ⚠️ 手机上符号层仍然只覆盖屏幕中部（`meet` 把方形画板放进竖屏，只占高度的 46%），最小一档也仍然很小。**真正的解法是给竖屏一套自己的布局**，但那要把 `BOARD` / `C` / `R_MIN` / `insideWedge` 全部参数化，是一次独立重构。

#### 手机上圆盘的字号

圆盘被压到 62vw（233px）之后**字号没有跟着缩**，于是记号从占圆盘 7.5% 变成 12.9%，所有者反馈「过于大了」。

现在：记号 `text-base sm:text-3xl md:text-4xl`（16px → 7.7%，与桌面的 7.5% 对齐），`--disc-label-size` 在 `max-width: 639px` 下降到 9px，标记与说明之间的间距 `gap-0.5 sm:gap-1`。

> ⚠️ **9px 是大写 Cormorant 的可读下限，不要再往下压。** 按比例说明文字本该是 5.8px，那不是一个能读的尺寸——所以这一档**故意不等比**。
>
> ⚠️ 英文在手机上仍然溢出约 5px（内外对称）：`PROFESSIONAL` 比手机上 58px 厚的圆环还宽，D19 的求解器只能把它配平，不能把它变窄。中文同视口下内外各余 20px，宽裕得多——**这是词长问题，不是布局 bug**。
>
> ✅ **桌面端零改动**，已实测：圆盘 480px、记号 36px、说明 12px、间距 4px、R 110.8、内外各 11.1——与改动前逐项一致。所有断点都是 `max-width` 或被 `sm:` 覆盖，只会变窄不会变宽。

#### 诗文的体例

所有者一次性给了 16 篇，「题目分类文体非常杂乱」，要求统一。**分三类**：`verse` 诗 / `prose` 文 / `letter` 信。

**标题规则**（这是全部混乱的来源，写死在这里）：

1. 作者用《》给了名字 → 那是标题，**存的时候去掉书名号**，强调交给排版
2. 作者只给了体裁标签（幽默小诗、随笔摘录、信件摘录）→ **那不是标题**，作品按无题处理，体裁由 `kind` 承担
3. 无题作品在索引里用**首行**指代，超过 24 字截断加省略号——有一篇是一句话重复九次不换行，索引必须还是索引

**组诗拆成独立篇目**，共用 `title` 用 `subtitle` 区分（无言的狂欢 · 组诗之一/二/三），所以它们读作「一件作品的三个部分」而不是三件无关的东西。

> ⚠️ **两首都叫《情诗》。** 现在是「情诗 一 / 情诗 二」——序号是为索引和 URL 做的消歧，**不是所有者给的标题的一部分**，他随时可以改。

> 🔴 **转录必须逐字。** 本轮我一度把《信件摘录 二》里引《百年孤独》的那段删掉了，等于擅自改稿——已全文恢复。规则见 D22 的红字：**别改所有者的字**，无论出于字体子集、长度还是任何别的理由。
>
> 已做的**仅限空白规范化**：行尾多余空格去掉；散文里明显是粘贴换行的断行（`啦啦的挽歌` 中句子中间那一处）接回。诗的断行一律原样。

**字体子集因此从 739 涨到 1179 字、381 KB。** 这是目前最大的一次跳变，再加中文要留意体积。

### D25 — 经历折叠、/coda 改版、精选与日期 ✅ 生效中（2026-08-17）

**三条线的经历条目折叠，`/cv` 保持平铺。** 用原生 `<details>`——无 JS 可展开、键盘可操作、无障碍标注自带，而且**浏览器的页内查找会自动展开命中的折叠块**，这几样自己写 toggle 都要额外做。
`EntryList` 加 `collapsible` 开关；三条线传 `true`，`/cv` 不传。

> ⚠️ **判断折叠是否生效要看 `<details>` 自身的高度，不要看内层元素。** 关闭状态下现代浏览器用的是 `content-visibility` 而非 `display:none`，内层 `getBoundingClientRect()` 照样有高度。实测：details 131px、summary 130px —— 这才是它确实收起来的证据。
>
> ⚠️ 默认三角标记不能定位，而这里的 summary 有三行，标记会黏在第一行。所以 `list-none` 关掉它，用 `.entry > summary::after` 画一个，`[open]` 时旋转。

**`/coda` 不再内嵌整首诗**，改为「生活自述（空）+ 精选七篇 + 一个强调过的入口」。精选名单在 `writing.ts` 的 `FEATURED`。

**空区块要留着并写明「待补」**，不要因为没内容就不渲染——有名字的空架子读作承诺，没有则读作什么都没有。`生活自述` 与 Creative 的 `音乐履历` 都是这个状态，等所有者填。

**诗篇日期**由所有者一次给定，写进各自的 `date`。《情诗 一》《情诗 二》《我的身体》**故意无日期**。《啦啦的挽歌》的 `date` 是作品年份 `~2026`，狗的生卒 `2010–2026` 移进了 `note`。

**首页右上角加邮箱。**

> 🔴 **邮箱用的是工作室地址 `elegistsstudio@gmail.com`，不是个人地址。** 公开页面上的邮箱一旦被爬走不可撤回。更好的长期方案是用域名自己的转发地址（Porkbun 的 MX 记录当初就是为此保留的，见 D20）。
>
> ⚠️ 完整地址要 228px，而同一行左边已经有名字，手机上必然相撞（实测 375 宽下 email x123..351、name x24..184）。所以 `md` 以下只显示短标签（邮箱 / Email），`md` 起显示完整地址。

### D26 — 符号层有两块画板：横板与竖板 ⚠️ 数值已被 D27 更新（两块画板的结构仍然生效）

> 🔴 **2026-08-19：横板改为 3200×2000，切换判据改为纯 `orientation`。** 见 D27。下表里 `wide` 那一列的 viewBox / rMin / reach / 缩放全部已过期，竖板不变。「两块都渲染进 DOM，用 CSS 选一块」「手摆元素不是等比缩放来的」这两条**仍然生效**。


**问题**（D13 的红字里已经写过一半）：方形画板配 `meet` 会按视口**短边**缩放。横屏窗口的短边是高度，一切正常；竖屏手机的短边是宽度，于是 2000×2000 的画板变成一个正方形贴在高屏幕中间——375×812 下只覆盖高度的 46%，而 `R_MIN = 580`（当初按「圆盘受 56vh 限制、占画板 56%」推出）落进了圆盘里，因为手机上圆盘改由宽度限制、占到 87%。**符号画在了圆盘底下。**

**现在**：竖屏另有一块 **1000×2000** 的画板。

| | 横板 | 竖板 |
|---|---|---|
| viewBox | 2000×2000 | 1000×2000 |
| `rMin` | 580 | 300 |
| `reach`（层半径倍率） | 1 | 0.62 |
| 375×812 下缩放 | 0.1875 | **0.375** |
| 覆盖高度 | 46% | **92%** |
| 字号渲染 | 4.5–11.6px | **9–23px**（桌面是 8.6–22px） |

> 🔴 **两块画板都渲染进 DOM，用 CSS 选一块显示；绝不能在 React 里按视口挑。** 那会让标记依赖服务端不可能知道的东西——正是 D15 存在的原因。切换规则：`@media (max-width: 767px) and (orientation: portrait)`。**横屏手机继续用横板**，那里它本来就成立（812×375 实测 R_MIN 109px 对外弧 90px）。

> ⚠️ **手摆的三个元素（波罗米结、两张 Creative 线稿）不是把横板的数值等比缩放得来的。** 两块画板长宽比不同，等比缩放会压扁它们。竖板那一套是另外摆的，并用求解器同一套规则（扇区角度 + 画板矩形 + rMin）逐角校验过——脚本化的校验结果三个都是 OK。
>
> 竖板落位：结 `{350, 60, 300×312}`；徽记 `{100, 1250, 150×212}`；人形 `{60, 1520, 300×363}`。

**实测落位数**（竖板 / 横板）：Scholarly 30 / 38，Professional 14 / 15，Creative 两张图都在。竖板窄，丢弃的条目更多——D13 的规则是**放不下就丢，空隙像构图、重叠像 bug**，这一条继续成立。

> ⚠️ **`reach` 是可调的，但调它要看落位数**：调大能往外伸，但竖板水平半宽只有 500 单位，伸太远反而被画板矩形挡掉更多。0.62 是当前取值。

### D27 — 首页改为波罗米环式三重 Venn，符号层铺满整页 ✅ 生效中（2026-08-19）

**由所有者提出**：「改成类似拉康三环的三重 venn diagram，保留现在的颜色，学术在下，创作在左上，实务在右上，悬停的时候整个圈铺开，盖满整个屏幕，然后每个面向的底板符号纹理铺满整个网页；profile 和下面的链接留出没有符号的空间。」

#### 为什么换掉圆环

环说的是**三者并列且分离**。它们并不分离——游戏是那个心理学家写的，工作室和研究是同一个人在跑。Venn 能说出环说不出的四个区域：三个两两相交，加正中间那一个。**中间那个就是他**，所以头像从「自己的一个中枢」变成「三者共有的那块」。

D9 的「中心不编码」仍然成立：那块区域不带任何一种颜料，它只是三者相遇的地方。

**几何**：三个圆心在半径 `CENTRE_D = 68` 的圆上、互隔 120°，每个圆半径 `R = 118`。圆心三角形边长 `68√3 ≈ 118`，约等于半径——这正是 Venn 通常的画法，也是让三重交叠大到能放下一张脸的比例。角度按 SVG 惯例（90° 在下）：学术 90°、创作 210°、实务 330°。

#### 🔴 三个圆不能填实心

D7 为了「三者平等」把三种颜料的感知亮度全部压暗过。**两层深色叠在近黑底上会变成一团泥，而交叠正是 Venn 的全部意义**——填实心等于把这个构图唯一想说的话涂掉。

现在：静止时骨白细描边 + 0.2 的颜料，点亮才到 0.55，被压暗的降到 0.07。**波罗米环本身就是线**，所以这不是妥协，是同一套语汇。

> 🔴 **填色和描边必须是两个元素。**
> 入场动画带 `fill-mode: both`，跑完会把它碰过的属性钉死在终值——所以一个动画 `fill-opacity` 的入场会把填色钉在实心上，下面那个 0.2 的静止值永远不生效。这是 D10「入场动画和淡出过渡不能挂在同一个元素上」的第三次复现（前两次：左上角简介、符号层的 drift）。
> 现在：入场挂在外层 `<g>` 上、动画的是普通 `opacity`；状态挂在形状上、动画的是 `fill-opacity` / `stroke-opacity`。**两个元素，两个属性，相乘。**

#### 水漫改由 CSS 圆定位，不再画进 SVG

光束必须**盖在符号层底下**，而符号层是 `fixed` 的一层，所以水漫也得是 fixed 元素。三个 `<span>` 用 `--disc` 把自己的圆心换算成视口偏移：

```
--disc: 圆盘的渲染宽度（globals.css 里声明一次）
translate(calc(var(--disc) * (cx - 200)/400), calc(var(--disc) * (cy - 200)/400))
```

**圆盘自己的 `max-width` 读的是同一个变量**，这是两者不会漂移的唯一保证。`z-index`：水漫 `-20`、符号层 `-10`，所以符号浮在颜料上。

#### 🔴 画板换成 3200×2000，因为方形盖不住横屏

`meet` 是把画板**内接**进视口的。2000×2000 在 1512×944 上只渲染 944 宽，**两侧各空 284px——40% 的屏幕没有纹理**。这不是没排满，是几何上排不到。

`slice` 能填满但要裁切，D13 的红字禁止它（会把结整个渲染到屏幕外），而且 `slice` 会让缩放比例从 0.472 跳到 0.756，D13 精调过、所有者确认过的字号档位全部放大 60%。

**3200×2000 同时解决两件事**：1512×944 下 `min(1512/3200, 944/2000) = 0.472`，**和方形画板在同一视口下的缩放完全相同**——每一档字号渲染出来一模一样，只是面积变大了。实测适配：1512×944 → 1510×944（只剩 2px 缝）；1280×800 → 严丝合缝；1920×1080 → 1728×1080；1024×768 → 1024×640。

- `reach` 拆成两个：`reach` 缩内半径、`reachOuter` 缩外半径。横板是 `1 / 1.35`——**内半径必须不变**（字要贴着圆盘边开始），外半径要够到 1887 单位的画角。两个一起缩会在圆盘周围开出一圈空白。
- **超宽屏仍然有边**：2560×1080 下两侧各空约 450px（`meet` 不裁切就必然如此）。但比方形画板的 740px 好得多，这是接受的代价。

#### 🔴 圆盘在横屏下按视口**宽度**封顶，这和 rMin 是一件事的两半

画板按 `min(vw/3200, vh/2000)` 适配，圆盘按高度缩放——**两者不是同一根轴，间距就会随窗口漂移**，`rMin` 只能按最坏情况取值（实测最坏 833 单位，而 1512×944 只需要 472），代价是每个正常视口都白白空出一大圈。

绑到同一根轴之后：`0.465 × 0.34vw × (3200/vw) = 506`，**恒定**，与窗口无关。高度那一支给出 `0.465 × 0.56vh × (2000/vh) = 521`。两者都在 `rMin = 580` 之下且有余量。

```css
@media (orientation: landscape)  { --disc: min(56vh, 30rem, 34vw); }
@media (orientation: portrait)   { --disc: min(30vh, 30rem, 62vw); }
```

> 🔴 **改 34vw / 30vh 之前必须重推 `symbol-field.tsx` 里的 rMin。** 它们是同一个约束的两半，而**两边都不会大声失败**——符号只是开始画到圆盘底下。
>
> 两条都不影响常见设备：1512×944 仍由 `30rem` 封顶（480px 不变），375×812 仍由 `62vw` 封顶（233px 不变）。它们只在小横屏窗口和竖屏平板上生效，而漂移正好发生在那里。

#### 竖板的切换判据改成纯 `orientation`

原来是 `max-width: 767px and portrait`，把竖屏平板留在了横板上。方形画板时代那样勉强活着（768×1024 下 rMin 223px 对外弧 221px，**只差 2px**）；换成 3200×2000 之后必死——它在 768×1024 上渲染成横在屏幕中间的一条 480px 窄带。

**方向才是诚实的判据**：决定哪块画板合适的是窗口的形状，不是它的大小。竖板在 768×1024 上渲染 512×1024（满高，两侧各空 128px），rMin 154px 对外弧 143px。

#### 禁区改为显式声明，且逐板不同

扇形遮罩没了，**四角不再天然安全**——左上角的简介和页脚的链接必须显式保留。所有者原话：「profile 和下面的链接留出没有符号的空间」。

> ⚠️ **禁区数值看起来过于慷慨，那是因为它们是按小视口标定的。**
> 简介块是**固定像素尺寸**，而画板的缩放随窗口变小——同一块文字在 1512×944 上占画板的 0.37，在 1024×768 上占 0.54。**按笔记本标定的禁区，在任何更小的窗口上都会让符号压进提示语。**

| | 横板（3200×2000） | 竖板（1000×2000） |
|---|---|---|
| 简介块 | `x < 0.56, y < 0.42` | `y < 0.20` 满宽 **＋** `x < 0.6, y < 0.32` |
| 右上邮箱 | `x > 0.70, y < 0.22` | （并进上面那条满宽带） |
| 页脚 | `y > 0.85` | `y > 0.85` |

**竖板的页眉要两个矩形**，因为两种竖屏设备穿法不同：手机的文案被 `max-w-xs` 卡在 375px 屏上，所以又宽又浅；平板是 `max-w-sm` 配 768px，而且保留了手机上被 `md` 藏掉的简介段落，所以又窄又深。一个矩形同时覆盖两者的话就是整个上三分之一。

> ⚠️ **手摆的三件（波罗米结、两张 Creative 线稿）不走 `insideField`，所以每次改禁区都要手动重新核对它们。** 本轮就漏了两次：结落在竖板的页眉带里（19 处碰撞），线稿落在横板的页眉禁区里（1 处）。
>
> 横板的结**没有放在上边缘**：那里被简介禁区（到 x=1792）和邮箱禁区（从 x=2240）夹成一条 448 单位的缝，而结宽 400——那不叫布局，叫塞。现在它在圆盘左侧、简介下方，四角半径 906–1338。

#### 提示语收进同一栏宽，并随简介一起在矮窗口隐藏

提示语是一整句话，大写 Cormorant 加字距把它排成 **515px 不换行的一行**——比它上面那段还宽，页眉块底部因此鼓出来一块。它同时是禁区必须围着长的东西，而**一行永不换行的字会让禁区随窗口变窄而一路长大**。

现在跟着上面两行走同一套 `max-w-xs / md:max-w-sm / xl:max-w-md`，并加上 `.hide-when-short`：812×375 下页眉本来会占掉半个屏幕高度，而画板在那里正是按高度适配的——为一行字守住半块画板，去保护一个没人会长时间保持的方向，不划算。**横屏手机是用点的，不需要被告知。**

#### 验证

375×812、768×1024、812×375、1024×768、1280×800、1366×620、1512×944、1920×1080、2560×1080，中英双语各测一遍：**落位 54–60 项，离屏 0，与任何真实文字元素（`header h1` / `header p` / `.email-link` / `footer a` / 圆盘标签 / 头像）碰撞 0。**

> ⚠️ **D19 那条「预览面板里改视口必须重新导航」已经不再需要。** 客户端测量随求解器一起删掉了，现在依赖视口的只有 CSS（选哪块画板、`--disc`），`resize_window` 之后直接量就是对的。已验证：1512×944 下不重新加载与重新加载的结果逐项一致。

> ⚠️ **`.focus()` 在预览面板里不会触发 React 的 `onFocus`。** 面板没有 OS 焦点时 `document.hasFocus()` 为 `false`，`HTMLElement.focus()` 只设 `document.activeElement`、**把 focus 事件推迟到文档重新获得焦点**。症状极具误导性：`activeElement` 确实变成了那个链接，状态却纹丝不动，看起来像状态机坏了。要测就直接派发 `new FocusEvent('focusin', { bubbles: true })`（React 17+ 就是靠 `focusin` 实现 `onFocus` 的）。

### D28 — 交叠处顺时针延续单一颜色；水漫只会张开 ✅ 生效中（2026-08-19）

D27 上线后所有者逐轮看图定下的，取代 D27 里关于交叠色的全部说法。

#### 🔴 交叠处取「一方的整色」，永远不混色

**混色试过两次，同一种方式失败两次。** 半透明叠加时 alpha 相乘，每一块交叠都比两个父色更暗；显式混色（screen、或任意色彩空间取平均）则发脏。

**这不是调参能救的问题，是这三种颜料本身的性质**：它们都暗（L\* 22–36），色相又互相隔得远——蓝配红是暗紫，蓝配金是橄榄，红配金是棕。**换到 OKLab 也一样**，只是脏得均匀一些。之后改成纯黑，脏是没有了，但读作「在圆盘上打了三个洞」——所有者原话「太不自然」。

**现行规则**：每块交叠整块取两个父色之一，按**顺时针**决定——每个圆的颜色向它顺时针方向的那块交叠延续过去。

| 交叠 | 位置 | 取色 |
|---|---|---|
| 创作 ∩ 实务 | 上中 | 创作（酒红） |
| 实务 ∩ 学术 | 右下 | 实务（旧金） |
| 学术 ∩ 创作 | 左下 | 学术（克莱茵蓝） |

**这正是让波罗米环成其为波罗米环的那条压叠规则**（见 D13 对结的说明）：每一对里有一个在上，而整个循环没有赢家。**所以中心只能是黑的**——没有哪个圆压在所有其他圆之上，属于三者的区域就不属于任何一个。D9 的「中心不编码」从此是**推出来的结论**，不是外加的规定。

> 🔴 **悬停判定必须跟着颜色走。** 每块交叠带的是它那个颜色主人的 `onMouseEnter` / `onPointerDown` / `onClick`——看起来是创作的区域，点下去必须就是创作。交叠块画在三个基圆之后，所以它天然是最上层命中目标；**不要给它加 `pointer-events-none`**，那会让命中落回基圆、由绘制顺序随机决定归属，颜色就在骗人。
>
> 中心那一块归**头像**（`hub`），不归任何一条线。它绝大部分被头像盖住，露出来的几个像素本来就是「他」。

**点亮时七块全部变成那一个颜色**（所有者要求「填满所有不是这个颜色的区域，这样最有视觉冲击力」），中心也不例外——一个正在变成整页的圆，不该在自己中心留个缺口。**静止态才是编码构图的那一态**，D9 在那里成立就够了。

#### 🔴 扇区之间移动只有「张开」，没有「收回」

原来旧色往回收、新色往外放同时发生，看到哪个方向取决于眼睛落在哪个圆上——所有者原话「有时候是一个圆往回收，有时候是一个圆往外放」。

**现行规则：颜料只在离开整个构图时才收回。** 扇区之间移动时旧色**停住**（`flood-hold`）、新色盖上去，全程只有一个张开。

> 🔴 **必须显式写 `z-index`。** 否则堆叠跟 DOM 顺序走，而 DOM 顺序是固定的 `TRACKS` 顺序——结果是往一个方向扫时旧色盖住新色、往另一个方向扫时又正常。张开的那个钉在 `-20`，被替换的那个 `-21`，两者都仍在符号层（`-10`）之下。

**水漫从 `scale(1)` 起、回到 `scale(1)`**，也就是圆盘里本来就在那儿的那个圆（被圆盘自己画的那一面挡着）。所以张开是**那个圆**长大，不是圆心冒出一个点；收回正好压回它身上，结尾没有任何东西可看。

> **晕染（液体消散）方案已废弃。** 所有者试过后不喜欢。保留记录是因为它的技术教训仍然成立：它把一个全屏 `filter: blur()` 和一个移动噪声遮罩放在了**每一次鼠标移开**的必经路上。`transform: scale` 是合成器唯一能白送的东西，而这个动作触发极其频繁。

#### 触屏停留时长由符号层反推，不是拍脑袋

`BEAM_HOLD = 1700`（触屏专用；指针设备直接跳转，那里加延迟只会读作卡顿）。

**下界由符号层决定**：纹理档最晚 520ms 才开始出现，每项淡入 620ms，所以最后一个符号在 **1140ms** 落定。原来的 1000ms 意味着路由在符号层收齐**之前**就切走了——这个停顿唯一想展示的东西，在手机上从来没有被完整看到过一次。现在多留 560ms。**改符号层的 `d0`/`d1` 就要重推这个值。**

#### 🔴 压叠处要断线，这才是真正的波罗米环

只把颜色让过去还不够——被压的那个圆的**线**也要在压叠区断掉，否则一整片连续的颜色中间横着一道骨白线。

**实现用 `<mask>`，不解析弧段**：每个圆一张遮罩，白色满幅 + 一个黑色圆盘（圆心是压着它的那个圆，半径 `R + BREAK`）。`BREAK = 3.5`，比线宽 1.5 倍还多，**断口必须比对方的线宽——正好断在对方线上会读作两条线相交，而不是一条从下面穿过去**。

每个圆被压且仅被压一次（`OVER` 就是 `CYCLE` 反过来读），所以：

- 学术压创作 → 创作的线在学术圆内断
- 创作压实务 → 实务的线在创作圆内断
- 实务压学术 → 学术的线在实务圆内断

**验证方法**（这套断言值得保留）：沿每个圆采样 3600 点，落在压它那个圆盘内的比例应当是 **124°**（纯几何交弧 120.1°，`BREAK` 再撑开 4°）；再逐对断言「恰好一个方向被断」——两个方向都断或都不断就不是纽结了。三项全过才算 Borromean。

**断线同时是颜色能连上的前提**：交叠块涂的是压方的颜料，而它与压方自身区域的边界，正好就是这里被切掉的那条弧。不切，一片连续的颜色中间就横着一条线。

> 🔴 **但中心那一块必须把线还回来。** 那一刀切的是整条弧，而这条弧里有一段正是**中心区自己的边界**——切掉之后中心成了一块完全没有轮廓的黑，绕着头像读作一个洞（正是两轮之前被否掉的那种观感）。
>
> 还线的办法是在遮罩里**再补一个白圆**：`白底 → 黑圆(OVER, R+BREAK) → 白圆(UNDER, R)`，三层按顺序覆盖。中心区的边界恰好就是「既在压方圆内、又在被压方圆内」的那一段，所以补 `UNDER` 这一个圆，还回来的**只有**那段弧，交叠块的接缝仍然是断的。
>
> **代价要知道**：两个内交点几乎正落在第三个圆的**圆心**上（圆心间距 117.8、半径 118，两者近乎重合），都落在被还回的白区里——所以中心闭合成一个完整的曲边三角形，**三个角上不带断口**。压叠关系仍然读得出来：外侧交点那里断口还在，而那正是肉眼看到「环互相穿过」的地方；颜料又说了第二遍（交叠块穿的是压方的颜色）。一块完全没有边的黑什么也没说。
>
> **验证**（沿每个圆采样 7200 点，按点落在几个其他圆内分类）：中心边界弧**画出 100%**、交叠接缝弧**画出 0%**、不在任何其他圆内的弧画出 98.9%——那缺的 1.1% 就是 `BREAK` 在外侧交点撑开的断口，是对的，不是漏画。

#### 🔴 CENTRE_D 是解出来的：三个圆与头像圆相切

所有者要求「三个圆往中心收拢的那条线进一步收拢，沿着头像框圆形的切线汇聚」。

圆心距中心 `CENTRE_D` 的圆，其最内点落在离中心 `R − CENTRE_D` 处，而那一点正是**中心区边界**所在。令它等于头像半径即得相切：

    R − CENTRE_D = R_PORTRAIT   →   CENTRE_D = 118 − 46 = 72

原来是 68，最内点在 50，比头像的 46 多出 4 个单位——中心因此戴着一圈松垮的黑边。现在三条弧正好贴着头像圆，头像成为曲边三角形的**内切圆**（切于三边中点）。

**三个数被锁在一起**：动头像就得动 `CENTRE_D`。要重新留出缝隙，是 `CENTRE_D < R − R_PORTRAIT`，不是随手挪一点。

> ⚠️ **连带影响：构图外缘从 186 涨到 190 画板单位（+2.2%），符号层的 `rMin` 必须重推。** 竖板从 300 提到 **315**（原来余量 12 单位，涨完只剩不到 2）。横板 580 不变（最坏 532，余 48）。**这类失败不会报错，只会让符号开始画到圆盘底下。**

#### 圆盘上的名字可以和页面上的不一样

英文 `PROFESSIONAL` 十二个字母，在圆盘上排成加宽大写后近角已经顶到相邻圆的弧。`TrackCopy` 新增可选的 `mark`：**只有圆盘**读它，缺省回落 `title`。

- 圆盘：`Practical`
- 履历页 `/en/professional` 的 h1、`/cv`、导航：仍然是 `Professional`

中文不受影响（实务两个字，本来就不挤）。

> ⚠️ 算一下再改这里的字：标签中心到相邻圆心 172.7，半径 118，所以**半个词最多 54.7 个单位**。`Practical` 是 70 单位宽（半宽 35，余 13.9），`PROFESSIONAL` 约 95 单位（半宽 47.5，余 1.4）——而在 `CENTRE_D` 还是 68 的时候那个余量是负的。

#### 图版按「CSS 实际显示哪块画板」预热，不是两块都预热

两块画板都在 DOM 里、CSS 只显示一块，所以原来空闲预热会把**六张**永远不会被看到的图也抓下来解码——约 220KB 传输、解码后好几 MB。

**这不违反 D15**：哪块画板可见是视口问题，D15 禁止的是**标记**依赖视口。`liveBoard` 在 render 和 hydration 期间一直是 `null`，服务端和客户端吐出完全一样的「无图」标记，答案只在 hydration 之后才给出。

> ⚠️ **那条媒体查询必须和 `globals.css` 里选画板的规则严格互补**（竖屏 → `tall`，其余 → `wide`）。两边一旦不同步，**可见的那块画板就是永远不加载图的那块**，而且不会报任何错。
>
> ⚠️ 监听了 `change` 以支持转屏。**这条在预览面板里测不了**——面板不合成帧，媒体查询的 `change` 事件根本不派发（和 D19 记的 `ResizeObserver` 不触发是同一个原因）。实测手段是**换尺寸后重新导航**，靠挂载时那次判断出结果：竖屏新挂载 → tall 6 张 / wide 0 张，横屏 → wide 6 / tall 0，两个方向都对。

> ⚠️ `window.requestIdleCallback` 的守卫要写成 `if (!idle)` 而不是三元。TS 把它的类型标成「一定存在」，`idle ? …` 会被判成 TS2774「你是不是忘了调用」。运行时守卫仍然必要（Safari 支持得晚）。

#### 四张学术图版改用扫图反色，不再手绘

所有者提供了原图（拉康 R.S.I.、荣格心灵结构、欲望图、格雷马斯方阵），要求「严格按照我给你的图片」，用与 DEAR SUSPECT 封面同样的骨白反色处理。

> 🔴 **手绘那一版失败在哪里，值得记住。** 我按参考图重画的 R.S.I. 里，第四个 Φ 环和开 *vrai trou* 的那道弧被画成了完整的圆——在原图里它们只是**擦过**结、且都跑出画框，而画成整圆之后它们直接横切过 Imaginaire 和 Réel，整个图不再读作三环互锁。所有者原话：「三界混进了奇怪的圆圈」。
>
> **这类稠密的既有图表不要凭参考图重画**，半径和相切关系差一点，图的意思就变了。扫图反色是对的做法。

**位置刻意打散**（所有者：「四张图片太规整」）。原来是整齐的 2×2、两边各两张且基线对齐；现在没有任何两张共用基线或左边缘，尺寸 430–700 拉开。**四张图不走 `insideField`**，所以改任何禁区或画板参数之后都要手工重核四角半径——这一点已经坑过两次。

## 5. 工作流约定

**不要直接改 `main`。** 标准循环：

```bash
git switch -c feat/描述性名字      # 开分支（分支只是指针，开销近乎为零，随便开）
# ... 改代码 ...
git add -A; git commit -m "feat: 说明"
git push                           # push.autoSetupRemote 已配置，无需 -u
gh pr create --fill
```

推送后自动发生：
1. **CI** 跑 `npm run lint` + `npm run build`，失败则阻止合并
2. **Vercel** 部署一个仅含该分支内容的预览网址，正式站不受影响
3. 确认无误后 `gh pr merge --squash --delete-branch` → 合入 main 触发正式部署

提交信息用 [Conventional Commits](https://www.conventionalcommits.org/)：`feat:` / `fix:` / `chore:` / `docs:` / `refactor:`。

## 6. 环境事实

> ⚠️ **所有者在多台机器上开发同一个仓库。** 本节以下的内容**逐机器不同**，读到时先确认当前机器是哪台，不要照抄。判断办法：`git rev-parse --show-toplevel` 看路径，`gh auth status` 看登录态。

### 跨机器不变的

- SSH 密钥 `~/.ssh/id_ed25519` 已注册到 GitHub，**无密码短语**，推送免交互
- 全局 git 配置已设 `init.defaultBranch=main`、`push.autoSetupRemote=true`、`core.longpaths=true`

### 机器 A（`C:\Users\shiyi\`）

- **`gh` CLI 不在该会话 shell 的 PATH 中**（winget 装后需新终端才刷新）。AI 调用时用完整路径：`C:\Program Files\GitHub CLI\gh.exe`
- `gh` 已登录，scope 为 `gist, read:org, repo`

### 机器 B（`C:\Users\Yixuan Shi\`）—— 2026-08-09 记录

- 仓库路径 `C:\Users\Yixuan Shi\dev\personal-site`（**在 OneDrive 之外**，故意的：`node_modules` 的硬链接会卡死 OneDrive 同步引擎）
- **`gh` 在 PATH 里，但没有登录**。SSH 推送正常（已验证认证为 `Coda-Shi`），但 §5 工作流里的 `gh pr create --fill` 会失败——要先 `gh auth login`，或者直接去网页开 PR
- **Windows「动画效果」是关闭的**，见 §7 的对应陷阱

## 7. 已知陷阱

> **隐私规则（最高优先级）**：本仓库是 Public。含个人信息的原始文档一律不入库，`.gitignore` 已封禁根目录的 `*.docx` / `*.doc` / `*.rtf` 和 `/private/`。**执行 `git add -A` 前必须先看 `git status`**——PII 一旦推送即被永久收录，删除提交也撤不回来。

| 陷阱 | 说明 |
|---|---|
| **Next.js 16 有破坏性变更** | 见 `AGENTS.md`。写任何 Next.js 代码前先读 `node_modules/next/dist/docs/`，**不要凭训练数据里的旧 API 写**。 |
| **PowerShell 5.1 管道会破坏 JSON** | 用 `$json \| gh api --input -` 会导致 HTTP 400。改用 `[System.IO.File]::WriteAllText($p, $json, (New-Object System.Text.UTF8Encoding $false))` 写无 BOM 临时文件，再 `--input $p`。 |
| **PowerShell 会吞 jq 的反斜杠** | `--jq '"\(.a)"'` 形式的字符串插值会解析失败。用简单表达式或直接取原始 JSON。 |
| **PowerShell 5.1 无 `&&`** | 用 `;` 或 `if ($?) { }`。 |
| **原生命令 stderr 被当错误** | git/gh 写 stderr 时 PowerShell 报 `NativeCommandError` 且 `$?` 为 false，**即使退出码是 0**。别据此判断失败。 |
| **`gh` token 缺 `workflow` scope** | 当前 scope 为 `gist, read:org, repo`。走 SSH 推送不受影响；若将来需通过 API 改工作流文件，需 `gh auth refresh -s workflow`。 |
| **Vercel 偶尔漏掉 push-to-main** | 合并后生产站迟迟不更新、而 CI 是绿的。判据不是「等久一点」,而是查**有没有部署被创建**:`gh api "repos/Coda-Shi/personal-site/deployments?sha=<merge-sha>" --jq length` 返回 `0` 就说明 webhook 根本没送到(commit status 会一直显示 `pending`,因为压根没有 status,不是因为在排队)。再对照 `curl -sI <生产站>` 的 `Age:`。补救:在 Vercel 面板点 Redeploy,或往 `main` 再推一个提交。**2026-08-20 发生过一次**,PR #35 合并后 Production 没有被创建。 |
| **超越函数会破坏 hydration** | `Math.sin` / `cos` / `atan2` / `hypot` 在 ECMA-262 里是实现自定义的，Node 与浏览器差最后一个 ulp。凡是会渲染成属性的计算结果都必须吸附到网格，见 D15。 |

> 🔴 **「入场动画不播放、所有东西直接闪现」——先查操作系统，不要改代码。**
>
> 2026-08-09 在机器 B 上遇到过一次，排查结论是**站点完全正常**：Windows 的「动画效果」被关掉了（设置 → 辅助功能 → 视觉效果 → 动画效果），Chromium 把这个系统设置映射成 CSS 的 `prefers-reduced-motion: reduce`，于是 §D11 那条全局规则如设计所愿把所有动画压到 `0.01ms`，配合 `fill-mode: both` 直接落在完成态——看起来就是「啪一下全出来了」。
>
> **诊断命令**（PowerShell，查 `SPI_GETCLIENTAREAANIMATION`，返回 `False` 即为关闭）：
>
> ```powershell
> Add-Type "using System;using System.Runtime.InteropServices;public class Spi{[DllImport(\"user32.dll\")]public static extern bool SystemParametersInfo(uint a,uint b,ref bool c,uint d);}"
> $v=$false; [void][Spi]::SystemParametersInfo(0x1042,0,[ref]$v,0); $v
> ```
>
> 浏览器里对应的一行是 `matchMedia('(prefers-reduced-motion: reduce)').matches`。
>
> ⚠️ **绝对不要为了「让动画能看见」去删或改 `globals.css` 里的 `@media (prefers-reduced-motion: reduce)` 规则。** 那条规则是无障碍功能，是给前庭功能障碍者用的（动画会引发眩晕和恶心），删掉是拿别人的健康换自己看一眼效果。要看动画就去把系统设置打开，或者在 Chrome DevTools 的 Rendering 面板里临时把 `prefers-reduced-motion` 模拟成 `no-preference`。
>
> 已验证过的性质：在 reduced-motion 生效时，页面落在**完成态**（`h1` 与 footer 的 opacity 均为 1），不是落在空白——D11 要求的这条仍然成立。把那条 media 规则从 CSSOM 里删掉后，`rise-in` 的时长立刻恢复成 700ms，说明动画本身毫发无损。

## 8. 当前进度

- [x] Git 环境与 SSH 打通
- [x] 安装并授权 `gh` CLI
- [x] Next.js 项目脚手架（lint + build 本地验证通过）
- [x] `.gitattributes`（统一 LF，防跨平台假 diff）
- [x] CI 工作流（PR 上跑 lint + build，首次运行 37s 通过）
- [x] 创建公开仓库并推送
- [x] 配置 `main` 分支保护
- [x] 连接 Vercel，Production 首次部署成功（2026-08-07 05:25 UTC）
- [x] 确定信息架构与视觉方案（见 §4、D7–D10）
- [x] 实现三条线内页、`/coda`、`/cv`
- [x] 首页重做为 trinity 圆盘（见 D10）
- [x] 首页改为波罗米环式三重 Venn，符号层铺满整页（2026-08-19，见 D27）
- [x] 交叠处顺时针延续单一颜色、水漫只张开、四张学术图版改用扫图（2026-08-19，见 D28）
- [x] 修掉 `SymbolField` 的 hydration mismatch（见 D15）
- [x] 中英双语骨架：`/[lang]/` 路由、`proxy.ts`、语言切换开关、中文字体（见 D16、D17）
- [x] 中文首页文案定稿并上线（简介、三条线名称与 lede、中心、提示语）
- [x] **中心照片**——彩色圆形头像已上线（见 D9 的红色警示与 `scripts/hub-portrait.py`）
- [x] 上线元数据：canonical、hreflang、OG 卡片、favicon、`robots.txt`、`sitemap.xml`（见 D18）
- [x] 删掉 `create-next-app` 留下的 5 个示例 SVG 和默认 favicon
- [x] 关闭 Vercel Deployment Protection（2026-08-12）
- [x] 购买并绑定域名 `yixuancodashi.com`（2026-08-15，见 D6、D20）
- [x] Vercel 设 `NEXT_PUBLIC_SITE_URL=https://yixuancodashi.com`（Production）并 Redeploy
- [x] 把部署网址写入仓库 homepage 字段
- [x] ~~标签半径改为运行时求解（见 D19）~~ —— 求解器已随 D27 删除，Venn 的月牙里不需要它
- [ ] 🔴 **把 Vercel 的 Preview 保护开回来**（Production 保持关闭）。2026-08-12 为了上线整个关掉了，Preview 一直敞着意味着未定稿的文案和半成品页面都能被人翻到。**所有者要求提醒他这件事**
- [x] **诗文 section**（Verse & Prose）已建，见 D22。现有一首，等所有者继续加
- [x] 上游 `ZhuqueFangsong-Regular.ttf` 已下回机器 A（v0.212），子集脚本可正常重跑
- [ ] 逐页打磨视觉（所有者原话「你先做，我们可以慢慢改」）
- [ ] 提交 sitemap 到 Google Search Console
- [ ] 符号层排布方案 A/B/C 待所有者选（见 D13）

**2026-08-15 上线验证**（全绿）：12 个页面全 200；OG 卡片 / icon / apple-icon 正常；sitemap 12 条且**无一条**指向旧地址；生产站无 `X-Robots-Tag` 也无 `robots` meta（可收录）；`www` 308 → 根域名 → 307 → `/en` → 200；旧的 `personal-site-kappa-eight-52.vercel.app` 307 → 正式域名。

### 中文版翻译进度

英文是规范版本，中文按节推进。`getDictionary()` 缺键回落英文，所以未译部分**不会开天窗**，只会显示英文。

**CV 条目的翻译机制**：`content.ts` 里每条记录带一个稳定的 `id`，`i18n.ts` 的 `entries` / `education` / `advocacy` / `skills` 按 id 覆盖，`localise()` 逐字段合并。**不要改用 org + period 作 key**——它们本身就是被翻译的对象，key 跟着文案动，结果是整条悄悄退回英文；简历上这种失败看起来像疏忽而不是 bug。

> ⚠️ `localise()` 的第二个参数必须包 `NoInfer<T>`。否则 TypeScript 会同时从两个参数推断 `T`，取交集后坍缩成 `{ id: string }`，其余字段在返回类型里全部消失。英文记录才是形状，覆盖层只需要能塞进去。

> 🔴 **每次加中文文案后必须重跑 `scripts/subset-fangsong.py`。** 子集只包含构建时存在的字符，新字会掉回系统字体。本轮翻译让字符数从 132 涨到 369、体积 37 KB → 113 KB。

| 区块 | 状态 |
|---|---|
| 首页简介 / 提示语 | ✅ 已定稿 |
| 三条线名称（学术 · 实务 · 创作） | ✅ 已定稿 |
| 三条线 lede | ✅ 已定稿 |
| 中心（Coda 其人 / 书写与行路之人） | ✅ 已定稿 |
| 导航与区块标题（简历 / 教育 / 行路 / 诗） | ✅ 已定稿 |
| CV 正文：学术 · 实务 · 创作 · 教育 · 行路 · 技能 | ✅ 2026-08-09 全部完成 |

**刻意保留拉丁文的机构名**（所有者确认）：`Everglory Products Corporation`（确实没有中文名）、`Crisis Text Line`（美国机构本名）。
~~`ELEGISTS STUDIO`~~ **已于 2026-08-16 更正——工作室的中文名是「唱诗人工作室」**，由所有者提供，现走 `dict.nav.studio`。**不要给它们编中文名**——翻译简历不是发明机构名的许可。
`XunZheng Mental Health Clinic` = **循证心理诊所**，`Hot Sound Club` = **热音社**，均由所有者提供。

工具与库名（lavaan、tidyverse、Power BI）同样保持拉丁文：那是这个领域的书写方式，译成中文只会更难读。
| `/cv` 的 Additional 区块标题（其他） | ✅ 已定稿 |
| 符号装饰层（`SYMBOL_LAYERS`） | 🚫 **按所有者要求永不翻译**——拉丁箴言、德语引文、心理测量记号保持原文 |

### 下一步队列（按优先级）

1. **CV 正文翻译**。条目在 `content.ts` 的 `TRACKS[].entries`、`EDUCATION`、`ADVOCACY`、`SKILLS`。译文进 `i18n.ts` 的 `zh`，需要先扩 `Dictionary` 类型。**工作量最大的一块，但纯机械。译完必须重跑 `scripts/subset-fangsong.py`**，否则新字符会掉回系统字体。
2. ~~朱雀仿宋 vendore 进仓库~~ ✅ 2026-08-09 完成，见 D17。
3. **中文标点空隙**。全角顿号句号在 lede 字号下空得明显，且会把「迷。」这类孤字甩到下一行。可选 `text-spacing-trim: trim-start`（Chrome 支持）；`hanging-punctuation` 目前只有 Safari 支持。所有者尚未定夺。

## 8.5 换机器怎么接上

> 这一节是给「明天换台电脑继续改」准备的。**照着做，别跳。**

```bash
git clone git@github.com:Coda-Shi/personal-site.git
cd personal-site
npm install
npm run dev
```

然后**必须做这三件事**，它们都存在 `.git/config` 或本机环境里，**不随仓库同步**：

1. 🔴 **设提交邮箱**，否则真实邮箱会进公开历史（§7 隐私规则，最高优先级）：

   ```bash
   git config --local user.email "257181346+Coda-Shi@users.noreply.github.com"
   ```

   > 顺带：账号级的根治办法是 GitHub → Settings → Emails 打开 **Keep my email addresses private** 和 **Block command line pushes that expose my email**，一次设置对所有机器生效。**2026-08-09 时这两项还是关的**，所以 #1–#4 的提交作者都带着真实邮箱。已经泄漏的撤不回来，但可以止损。

2. **确认 `gh` 登录状态**：`gh auth status`。没登录的话 `gh pr create` 会失败，得先 `gh auth login`，或者直接去网页开 PR。

3. **`node_modules` 必须在 OneDrive 之外**。硬链接会卡死 OneDrive 同步引擎——所以仓库放 `~/dev/`，不要放进 OneDrive 目录。

验证环境没问题：`npm run lint && npm run build`，应当零报错、静态生成 12 个页面（6 条路由 × 2 个 locale）。

## 9. 待决事项

| 问题 | 状态 |
|---|---|
| 🔴 **Vercel 的 Preview 保护还开着吗？** | 2026-08-12 为了上线，Deployment Protection **整个**关掉了（Production + Preview）。Production 必须保持关闭，但 **Preview 应当开回来**——否则每个 PR 的预览部署都是公开的，未定稿文案、半成品页面都能被人翻到。**所有者明确要求提醒他这件事。** 位置：Vercel → 项目 → Settings → Deployment Protection → Vercel Authentication，只对 **Preview** 启用。 |
| 全局 `user.email` 是否也改为 noreply？ | **未定**。改了则所有新仓库默认匿名，但影响机器上全部项目。已向所有者提出，等答复。 |
| 网站要有哪些内容/板块？ | 未讨论。设计方向、信息架构、视觉风格全部空白。 |
| 是否加备用邮箱到 GitHub 账号？ | 未做。GitHub 已警告只有单一验证邮箱，丢失即无法找回账号。 |
